# ADR-20260524 wave252 — v0.99.17 Phase 85 close

## Bağlam

Phase 85, marathon'un 88. tag'i (patch level). RFC 6298 EWMA RTT
stats + true ECS archetype overlap detection.

## Karar

### 85.A — `cd/net/LatencyStats.hpp`
RFC 6298 RTT smoothing:
- `current_rtt_us` — EWMA (α = 0.125).
- `jitter_us` — EWMA of |diff| (α = 0.25).
- `min/max/sample_count`.
- `reset`.

UDP connection quality telemetry için. ChannelMux / UdpConnection
ileride bu primitive'i timestamp/ack roundtrip ile besler.

### 85.B — `cd/ecs/ComponentMask.hpp`
Phase 75 QuerySig XOR-set hash overlap'a cevap veremiyordu;
ComponentMask her component type'a unique bit atar:
- `mask_of<T>()` — atomic counter ile sıralı bit atama.
- `make_mask<T...>()` — OR.
- `masks_overlap(a, b)` — true iff `a & b != 0`.
- `mask_subset(sub, super)`.

System scheduler conflict detection: write-mask'leri overlap eden
sistemler aynı tick'te paralel koşamaz.

Sınır: 64 component type per process. 64+'lık projeler için
`Bitset` (Phase 34) ile genişletilebilir.

## Test

- LatencyStats: FirstSampleBecomesRtt, EwmaSmoothsAcrossSamples (spike
  attenuation), JitterTracksVariation, MinMaxTracked — 4 case.
- ComponentMask: EachTypeGetsUniqueBit, MakeMaskCombinesTypes,
  OverlapDetected, NonOverlapDistinct, SubsetCheck — 5 case.

`ctest --preset ninja-debug` → **58/58 PASS**.

## Sonuçlar

- 58 ctest yeşil. Tag: **v0.99.17**.
- Net telemetry için connection-quality stats.
- ECS scheduler conflict resolution için doğru primitive.
