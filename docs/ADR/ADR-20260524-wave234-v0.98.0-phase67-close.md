# ADR-20260524 wave234 — v0.98.0 Phase 67 close

## Bağlam

Phase 67, marathon'un 70. minor release'i. Core retry policy + editor
camera bookmark primitive'leri.

## Karar

### 67.A — `cd/core/RetryPolicy.hpp`
- `RetryPolicy { max_attempts, base_delay_ms, max_delay_ms, factor }`.
- `attempt_delay_ms(n)` — exponential `base * factor^(n-1)`,
  `max_delay_ms` cap.
- `should_retry(n)` — `n < max_attempts`.

IO-friendly counterpart to Backoff (Phase 40, CPU-spin). Asset loader,
network reconnect, HTTP fetch retry pattern için.

### 67.B — `cd/editor/Bookmark.hpp`
- `Bookmark { name, eye, target, up }` snapshot.
- `BookmarkSet.add(name, cam)` / `apply(i, cam)` / `remove(i)`.
- `cd::editor` `cd::camera` PUBLIC_DEPS'e eklendi.

Designer "front / top / inside-cockpit / best-shot" view snap. Phase 38
CameraPath timeline-driven; Bookmark snap-jump variant.

## Test

- RetryPolicy: FirstAttemptUsesBaseDelay, ExponentialBackoff,
  ClampsAtMaxDelay, ShouldRetryRespectsMaxAttempts — 4 case.
- BookmarkSet: AddAndApply, ApplyOutOfRangeRejected, RemoveCompacts — 3 case.

`ctest --preset ninja-debug` → **58/58 PASS**.

## Reddedilen alternatifler

- **RetryPolicy için jitter (full / decorrelated)**: AWS retry pattern
  iyileştirir ama core dep tree'ye RNG inject etmek gereksiz. Caller
  Random'la birleştirir.
- **Bookmark'ı CameraPath ile birleştirmek**: keyframe slot
  + transition timing CameraPath; snap-only Bookmark. Ayrı concerns.

## Sonuçlar

- 58 ctest yeşil. Tag: **v0.98.0**. **70. release**.
- IO retry pipeline parameter düzeyinde reusable.
- Editor camera workflow için bookmark + path tam çift.
