# ADR-20260524 wave239 — v0.99.4 Phase 72 close

## Bağlam

Phase 72, marathon'un 75. tag'i (patch level). Input action-binding
map + 2D AABB.

## Karar

### 72.A — `cd/input/Bindings.hpp`
Action-name (FNV-1a 32-bit hash) → `vector<KeyCode>` binding map:
- `bind("MoveLeft", KeyCode::kA)` + `bind("MoveLeft", KeyCode::kLeft)`.
- `is_down(action, state)` — true iff any bound key held.
- `unbind_all(action)` / `action_count()` / `clear()`.

KeyChord (Phase 45) modifier-exact event-on-press; Bindings sustained
game-loop held-key check.

### 72.B — `cd/math/Aabb2.hpp`
Float 2D AABB:
- `contains(box, x, y)` / `overlaps(a, b)` / `merge(a, b)`.
- `expand(box, amount)` / `area(box)`.

3D Aabb (Phase 20) muadili — UI screen-space + 2D scene için.

## Test

- ActionBindings: BindAndCheckIsDown, MultipleKeysForOneAction,
  UnbindRemovesAll — 3 case.
- Aabb2: ContainsRespectsBounds, OverlapsBox, MergeUnions,
  ExpandPadsSymmetric, AreaProduct — 5 case.

`ctest --preset ninja-debug` → **58/58 PASS**.

## Sonuçlar

- 58 ctest yeşil. Tag: **v0.99.4**.
- Gameplay action mapping (move/jump/use) tek primitive ile.
- 2D math katmanı 3D Aabb ile aynı kalitede.
