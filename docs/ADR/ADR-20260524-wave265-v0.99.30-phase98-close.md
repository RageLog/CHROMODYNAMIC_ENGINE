# ADR-20260524 wave265 — v0.99.30 Phase 98 close

## Bağlam

Phase 98, marathon'un 101. tag'i — 100-tag milestone'undan sonraki
ilk release. Rhi raster-state preset koleksiyonu + asset reference
counter.

## Karar

### 98.A — `cd/rhi/RasterStatePresets.hpp`
4 yaygın raster reçete:
- `raster_solid_back()` — default mesh.
- `raster_solid_none()` — transparent / two-sided.
- `raster_wireframe()` — debug overlay (line polygon mode, no cull).
- `raster_shadow_front()` — shadow caster pass (front cull).

BlendPresets (Phase 34) + DepthStencilPresets (Phase 43) ile beraber
PSO build sitelerinin 3'lü preset choice'ını tamamlıyor.

### 98.B — `cd/asset/AssetRefCount.hpp`
- `acquire(id)` / `release(id)` → new count.
- `count_of(id)` / `is_held(id)` / `tracked_count()`.
- Over-release defensive (negatif olmaz).
- Invalid AssetId rejected.

Hot-reload safety: asset count > 0 iken reload deferred / atomik
swap; count = 0 iken safe unload.

## Test

- RasterStatePresets: SolidBackIsDefaultCullBack, SolidNoneDisablesCull,
  WireframeUsesLineMode, ShadowCullsFront — 4 case.
- AssetRefCount: AcquireIncrements, ReleaseDecrements, OverReleaseSafe,
  InvalidIdRejected — 4 case.

`ctest --preset ninja-debug` → **58/58 PASS**.

## Sonuçlar

- 58 ctest yeşil. Tag: **v0.99.30**. **101. release**.
- Renderer PSO state choices artık 3-preset library (blend/depth/raster).
- Asset hot-reload pipeline için reference safety yerinde.
