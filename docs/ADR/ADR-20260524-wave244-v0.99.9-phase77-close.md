# ADR-20260524 wave244 — v0.99.9 Phase 77 close

## Bağlam

Phase 77, marathon'un 80. tag'i (patch level). Text layout estimator +
Promise/Future çift primitive'i. **v0.99.9 — patch space'in mecazi
"son" hanesi**. Bundan sonra eklenecek phase'ler ya `v0.99.10` ya
da kullanıcı v1.0 onayı verir.

## Karar

### 77.A — `cd/render/TextLayoutMetrics.hpp`
- `TextLayoutMetrics { width, height, line_count }`.
- `measure_simple(text, font_size_px, char_advance_px=7)` — monospace
  estimation, `\n` line break.

Editor inspector tooltip / button text size hesabı için. Real font
metrics (FreeType / DirectWrite) backend ileride bu API'yi dolduracak.

### 77.B — `cd/concurrency/Future.hpp`
Light std::future muadili:
- `Promise<T>` + `Future<T>` shared `FutureState<T>` üzerinde.
- `Promise.set(v)` consumer'ları uyarır.
- `Future.is_ready()` / `try_get()` (optional) / `wait()` / `get()`.

`std::future`'a kıyasla:
- Throw etmiyor — ready durumunda T kopyası.
- Multi-consumer OK (value copyable).

Asset cook → editor wait, async shader compile → renderer ready.

## Test

- TextLayoutMetrics: EmptyTextZero, SingleLineMonospace,
  NewlineIncrementsLineCount — 3 case.
- Future: SetMakesReady, TryGetBeforeSetIsEmpty, WaitUnblocksOnSet
  (cross-thread sleep 2ms + set/wait) — 3 case.

`ctest --preset ninja-debug` → **58/58 PASS**.

## Sonuçlar

- 58 ctest yeşil. Tag: **v0.99.9**. 80. release.
- Editor UI altyapısı için text size estimation tek noktada.
- Engine async iş için Promise/Future çift kontratı yerleşti.

**Marathon notu:** v0.99 patch space'inin "natural" stop noktasına
geliyoruz. Sonraki phase'ler v0.99.10 (yine patch) veya v1.0.0 (user
sign-off bekler — bu yetki marathon'a verilmemiştir).
