# ADR-20260524 wave253 — v0.99.18 Phase 86 close

## Bağlam

Phase 86, marathon'un 89. tag'i (patch level). UI tab-bar + bitwise
enum operator macro.

## Karar

### 86.A — `cd/ui/TabBar.hpp`
Headless tab-bar:
- `Tab { id, label }`.
- `add(id, label)` — first add sets active.
- `set_active(id)` / `next()` / `prev()` (wraps).
- `close(id)` — drops + adjusts active.
- `active()` / `active_index()` / `size()` / `tabs()`.

Editor multi-scene tab bar, dockable panel tab strip.

### 86.B — `cd/core/EnumFlags.hpp`
`CD_ENUM_FLAGS(EnumType)` makrosu:
- `|`, `&`, `~`, `|=`, `&=` operatörlerini constexpr olarak inject eder.
- `cd::core::has(set, bit)` — non-zero test, namespace'de.

`enum class : T` ile typed bitmask kullanımını cast-free yapar.
Render flag bitmask'leri, GPU pipeline state bit set'leri için.

## Test

- TabBar: AddSetsFirstActive, SetActiveByIdSwitches,
  NextPrevWrapsAround, CloseAdjustsActive — 4 case.
- EnumFlags: OrCombines, AndIntersects, NotInverts, AssignmentOps — 4 case.

`ctest --preset ninja-debug` → **58/58 PASS**.

## Sonuçlar

- 58 ctest yeşil. Tag: **v0.99.18**.
- Editor multi-document UI için tab-bar pure-data backing.
- Enum-flags boilerplate'i tek makro ile elimine.
