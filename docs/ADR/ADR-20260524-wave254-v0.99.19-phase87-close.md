# ADR-20260524 wave254 — v0.99.19 Phase 87 close

## Bağlam

Phase 87, marathon'un 90. tag'i (patch level). Anim root-motion
alignment + render clear-color preset paleti. **90th release**.

## Karar

### 87.A — `cd/anim/PoseAlign.hpp`
Root-motion retargeting plumbing:
- `align_root_position(frames, rest_root)` — frame.position -= rest.
- `compose_root_motion(frames, world_root)` — frame.position += world.
- `root_velocity(prev, current, dt)` — Vec3 finite-difference.

Position-only — rotation/scale değişmez. Retargeting + locomotion
preserve-velocity için.

### 87.B — `cd/render/ClearColorPreset.hpp`
Named clear colors (4-comp linear RGBA):
- `clear_black`, `clear_white`, `clear_cornflower_blue` (XNA/production engine
  default), `clear_editor_gray` (#1E1E1E), `clear_debug_magenta`.

Magenta intentionally garish — debugging "renderer ne yapıyor"
visual signal.

## Test

- PoseAlign: AlignSubtractsRestRoot, ComposeAddsWorldRoot,
  VelocityFromPosDelta (10/0.5 = 20 m/s), VelocityZeroDtSafeFallback — 4 case.
- ClearColor: BlackIsZero, CornflowerBlue, DebugMagentaIsObvious — 3 case.

`ctest --preset ninja-debug` → **58/58 PASS** (cached_compiler TU bir
kez transient clang frontend crash — retry yeşil; reproducible
değil).

## Sonuçlar

- 58 ctest yeşil. Tag: **v0.99.19**. **90. release**.
- Retargeting / locomotion pipeline için root-motion helper.
- Renderer color palette tek noktada.
