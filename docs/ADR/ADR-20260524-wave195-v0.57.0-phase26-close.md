# ADR — v0.57.0 / Wave 195 — Phase 26 close

- Date: 2026-05-24, Wave 195, Tag: v0.57.0.

## Shipped
- 26.A `cd::core::RingBuffer<T, N>` — fixed-capacity FIFO (3 tests).
- 26.B `cd::math::CatmullRomSpline` — N-CP spline `at(t)` (2 tests).
- 26.C `cd::physics::Capsule` + `contains(point)` (2 tests).
- 26.D `cd::concurrency::Once` — `std::call_once` wrapper (2 tests).

26.E (hello_random sample) cycles to Phase 27.

29 minor releases (v0.30 → v0.57). Full ctest (58/58) green.
