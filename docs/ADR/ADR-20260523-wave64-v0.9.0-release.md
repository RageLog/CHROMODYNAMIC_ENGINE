# ADR-20260523 — Wave 64: v0.9.0 release tag (Phase 6 Sprint 4)

## Bağlam

Gece marathon dördüncü sprint'i kapanır. Sprint 4 sadece 3 wave (61-63)
+ tag, çünkü kalan iş tek dosyalık dokümantasyon/CI entegrasyonu
ağırlıklı; ayrı bir sprint açmak yerine "feature complete" yolda kalan
ufak parçaları topladı.

Sprint 1-3 (Wave 37-59) yeni library yüzeyleri ekledi; Sprint 4 mevcut
yüzeyleri parlatıp dokümantasyon/CI tarafını kapadı.

## Karar

`v0.9.0` git tag'i atılır. Phase 6'nın "kalanın küçük olanı"
snapshot'ı — 1.0 öncesi son minor bump beklenebilir (Phase 6 routing
tablosundaki açık kalanlar Phase 7'ye yönlendirilirse). 4/4 Windows
compiler × 54/54 ctest green.

Tag dev branch HEAD'ine; master merge ayrı PR.

## Bu sürümde olanlar (delta v0.8.0 → v0.9.0)

### Phase 6 Sprint 4 waves (61-63)

| Wave | Library | Çıktı |
|---|---|---|
| Wave 61 | `cd::imgdiff::compute_ssim_gaussian` | Gaussian-weighted SSIM (Wang 2004 §III-B reference) — 2D Gaussian window σ=1.5 (11×11), stride-configurable; FLIP perceptual-diff foundation üzerine inşa edildi |
| Wave 62 | `cd::net::ReliableChannel` | Window-based flow control — `send_window_size` cap, kWouldBlock back-pressure; TCP-style "peer drain bekle" davranışı (cwnd/AIMD hariç) |
| Wave 63 | `CHANGELOG.md` + `.github/workflows/changelog-check.yml` | Repo-root CHANGELOG.md (Wave 58 tool ile üretildi v0.2 → v0.8); per-PR CI smoke job tool'u Linux'ta build edip range pipe ediyor |

### Kümülatif sayılar

| Metric | v0.8.0 | v0.9.0 |
|---|---|---|
| Engine libraries (cd_add_library) | 47 | **47** |
| Samples | 33 | **33** |
| Tools | 5 | **5** |
| ctest binaries | 54 | **54** |
| Internal test cases | ~195 | **~200** (+SSIM Gaussian 4, +Window flow 1) |
| 4/4 Windows compilers | 4/4 | **4/4** |
| Phase 6 waves toplam | 22 (37-59) | **25 (37-63)** |
| GitHub Actions workflows | 2 (ci, docs) | **3** (+changelog-check) |
| Root-level docs | docs/ADR + CLAUDE.md + README | **+CHANGELOG.md** |

### Yeni public API yüzeyleri

```
cd::imgdiff::compute_ssim_gaussian       (Wave 61)
cd::net::ReliableChannel::send_window_size getter/setter (Wave 62)
CHANGELOG.md root file                   (Wave 63)
.github/workflows/changelog-check.yml    (Wave 63)
```

### Reviewer tek-komut akışı (v0.9.0)

```bash
git clone https://github.com/RageLog/CHROMODYNAMIC_ENGINE.git
cd CHROMODYNAMIC_ENGINE
git checkout v0.9.0

cmake --preset ninja-base -DCD_DISABLE_VCPKG=ON
cmake --build --preset ninja-debug
ctest --preset ninja-debug                    # 54/54

cat CHANGELOG.md                              # Conventional Commits log
build/ninja-base/bin/Debug/hello_events.exe   # Sprint 3 stack demo
build/ninja-base/bin/Debug/cd_bench_compare.exe baseline.json current.json
```

## Phase 6 routing kapı listesinden tamamlananlar (Sprint 1-4 toplam)

- ✓ Real UDP IConnection backend                           (Wave 37)
- ✓ Retransmit + ACK basic                                 (Wave 41)
- ✓ Adaptive RTO (RFC 6298 SRTT/RTTVAR)                   (Wave 45)
- ✓ Cumulative ACK                                         (Wave 46)
- ✓ Selective ACK + fast retransmit                        (Wave 53)
- ✓ Window-based flow control (back-pressure)              (Wave 62)
- ✓ Native file watcher trifecta (Win/Linux/macOS)         (Wave 38/47/51)
- ✓ Perceptual diff foundation (SSIM-lite + Gaussian blur) (Wave 39/54)
- ✓ Gaussian-weighted SSIM (FLIP-lite)                     (Wave 61)
- ✓ Bench Markdown + JSON + diff tool                      (Wave 40/48)
- ✓ JSONL log sink                                         (Wave 55)
- ✓ Deferred event bus                                     (Wave 56)
- ✓ Frame pacer + interval ticker                          (Wave 57)
- ✓ Conventional Commits → CHANGELOG.md                    (Wave 58/63)
- ✓ ECS query stale-detection                              (Wave 42)
- ✓ Render thread soak harness                             (Wave 50)
- ✓ CHANGELOG.md formalisation                             (Wave 63)
- ✓ Changelog generator CI smoke step                      (Wave 63)

## Phase 6 routing kapı listesinden hâlâ açık

- ✗ Forward+ / clustered light culling
- ✗ Volumetric fog
- ✗ Mobile RHI (Metal, D3D12)
- ✗ GPU compute IBL bake
- ✗ Real CoreAudio implementation       (factory stub Wave 34'ten beri)
- ✗ Real ALSA implementation            (factory stub Wave 34'ten beri)
- ✗ 3D positional audio + HRTF
- ✗ WebRTC / SteamNetworkingSockets bridge
- ✗ Congestion control (cwnd / AIMD)
- ✗ Symbol-level hot-patch
- ✗ Scripting tier (Lua / WASM)
- ✗ Performance regression bench CI step (tool var, bench step yok)
- ✗ Multi-day soak run + leak report (harness var, gerçek soak yok)
- ✗ Full FLIP perceptual diff (CSF + spatial filtering)

## Reddedilen alternatifler

- **Sprint 4'ü v0.8.x patch'e ezmek:** Yeni `compute_ssim_gaussian`
  + `send_window_size` API yüzeyleri → minor bump (v0.9.0 doğru).
- **Phase 7'ye doğrudan geçmek:** v0.9.0 hâlâ Phase 6; routing
  tablosunda 14 açık item var. Phase 7 = "production-ready"
  iddiası, v1.0 hak ediyor.

## Sonuçlar

`v0.9.0` annotated tag, dev HEAD üzerine. Phase 6 Sprint 4 closure.
Networking yüzeyi: feature-complete for v1.0 (cwnd hariç). Perceptual
diff yüzeyi: foundation tamamlandı (FLIP CSF + spatial Sprint 5+).
Tooling yüzeyi: bench compare + changelog gen + CI smoke kapı listesi
karşılandı.

Sıradaki sprint'ler (eğer marathon devam ederse, Phase 6 → 7 geçişi):
1. Forward+ / clustered light culling (Phase 7 birinci item)
2. Scripting tier (Lua via FetchContent)
3. Real CoreAudio / ALSA (CI runner gerekli)
4. Mobile RHI başlangıcı (Metal via Vulkan Portability)
5. Full FLIP perceptual diff (CSF + spatial)

Phase 7 = "v1.0 path" mimari ADR'si Phase 6'nın gerçek closure'ı
olacak.

## Açık sorular

* **Phase 7 nereden başlasın?** Kullanıcı uyandığında 5 candidate
  listesinden seçecek; en görünür kalite sıçraması Forward+/mobile
  RHI, en yüksek developer-experience scripting tier, en derin
  audio CI runner.
* **CHANGELOG.md elle güncelle vs auto?** v0.9.0'da CHANGELOG.md
  manuel re-gen + commit ile beslendi. Otomatik (release-only)
  step CI'a eklenebilir; şu an manuel/Wave 63 sonrası göreceli
  düşük yük.
* **CD_BENCH_REGRESSION CI step:** Wave 48-49 zinciri hazır; CI'a
  step eklenmediği için PR'lar otomatik regress alarmı vermiyor.
  Phase 7 candidate.
