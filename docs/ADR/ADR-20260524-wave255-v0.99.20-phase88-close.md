# ADR-20260524 wave255 — v0.99.20 Phase 88 close

## Bağlam

Phase 88, marathon'un 91. tag'i (patch level). Net QoS tier enum +
physics collision contact-point manifold.

## Karar

### 88.A — `cd/net/QoSTier.hpp`
4-tier priority enum: Low / Normal / High / Critical:
- `tier_priority(t)` — int score, higher = more urgent.
- `may_drop_on_congestion(t)` — Low/Normal true; High/Critical false.
- `requires_reliable_delivery(t)` — High/Critical true.

Send scheduler ileride bu enum'a göre packet prioritize eder.

### 88.B — `cd/physics/ContactPoint.hpp`
- `ContactPoint { world_position, normal, penetration, tangent_friction }`.
- `ContactManifold` — fixed 4-slot manifold (box-box / box-sphere için
  yeterli).
- `add(p)` (cap 4), `at(i)`, `size`, `empty`, `clear`.

Collision detection output kontratı. Jolt entegrasyonu sırasında bu
manifold ile beslenecek.

### Environment note
Build sırasında "paging file too small" Windows hatası alındı —
clang-cl parallel ninja N=8 paging'i tüketiyor. `cmake --build -j2`
ile aşıldı. Marathon system-level limit'i; primitive değil.

## Test

- QoSTier: CriticalIsHighestPriority, LowMayDrop, HighRequiresReliable — 3 case.
- ContactManifold: EmptyByDefault, AddAccumulates, CapacityRejectsOverflow,
  ClearEmpties — 4 case.

`ctest --preset ninja-debug` → **58/58 PASS**.

## Sonuçlar

- 58 ctest yeşil. Tag: **v0.99.20**.
- Multiplayer channel QoS tagging için tek enum.
- Collision manifold veri kontratı yerinde.
