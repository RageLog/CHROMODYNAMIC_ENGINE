# ADR — v0.53.0 / Wave 187 — Phase 22 close

- Date: 2026-05-24
- Status: Accepted
- Wave: 187
- Tag: v0.53.0

## Sub-phases shipped (all five)

### 22.A — `cd::scene::SpatialHash<T>`
Uniform-grid neighbour query. `insert(item, pos)` + `query_sphere(center, radius, out)`. 2 tests.

### 22.B — `cd::input::Axis`
Multi-source normalized axis [-1, 1] from key-pair or analog
override. 4 tests.

### 22.C — `cd::physics::Ray` + `intersect_ray_aabb`
Slab-method ray/AABB intersection returning entry t. 3 tests.

### 22.D — `cd::math::CubicBezier`
4-control-point Bezier `at(t)` + `arc_length(samples)`. 2 tests.

### 22.E — `cd::core::FixedString<N>`
Fixed-capacity non-allocating string for constexpr-friendly
labels. 4 tests pinning empty/assign/truncate/equality.

## Version

0.52.0 → 0.53.0. 25 minor releases cumulative.

## References

- ADR-20260524-wave186-phase22-plan.md
- 5 new headers: SpatialHash.hpp / Axis.hpp / Ray.hpp /
  CubicBezier.hpp / FixedString.hpp
