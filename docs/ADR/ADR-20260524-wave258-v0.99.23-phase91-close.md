# ADR-20260524 wave258 — v0.99.23 Phase 91 close

## Bağlam

Phase 91, marathon'un 94. tag'i (patch level). Input long-press
detector + Vec3 sRGB ↔ linear conversion (+ γ=2.2 approx).

## Karar

### 91.A — `cd/input/Hold.hpp`
- `set_threshold(seconds)` (default 0.5).
- `on_press(now)` / `on_release()`.
- `is_held(now)` — pressed AND duration ≥ threshold.
- `held_duration(now)` — pressed olduğunda press'ten beri geçen
  saniye, aksi takdirde 0.
- `is_pressed()` — alt-state query.

Editor click-and-hold context menu, "hold W for sprint", "hold Esc
to confirm dismiss" pattern'leri için.

### 91.B — `cd/math/GammaSpace.hpp`
Color.hpp (Phase 23) zaten scalar `srgb_to_linear` / `linear_to_srgb`
sahip; bu header ek olarak:
- Vec3 overload'ları (component-wise loop).
- `*_approx` family — γ=2.2 simple pow, editor preview için ucuz
  alternatif (~%2 hata midrange'de).

## Test

- Hold: NotHeldBeforeThreshold, HeldAfterThreshold,
  ReleaseClearsHold — 3 case.
- GammaSpace: Vec3SrgbToLinearAppliesPerChannel,
  Vec3LinearToSrgbRoundtrip (round-trip <1e-4 error),
  ApproxGamma22 (within 5% of accurate) — 3 case.

`ctest --preset ninja-debug` → **58/58 PASS**.

## Sonuçlar

- 58 ctest yeşil. Tag: **v0.99.23**.
- Editor + gameplay hold gesture'i tek primitive.
- sRGB color math vectorized + approximate forms mevcut.
