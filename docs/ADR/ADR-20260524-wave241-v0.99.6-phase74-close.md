# ADR-20260524 wave241 — v0.99.6 Phase 74 close

## Bağlam

Phase 74, marathon'un 77. tag'i (patch level). Soft body tuning struct
+ scratch byte buffer.

## Karar

### 74.A — `cd/physics/SoftBodyParams.hpp`
Cloth / spring solver knobs:
- `stiffness` (Hooke constant), `damping` (viscous), `gravity` (Vec3),
  `iterations` (XPBD), `rest_length_scale`.
- `soft_body_cloth_default()` — düşük stiffness, orta damping.
- `soft_body_rope_default()` — yüksek stiffness, agresif damping.

Editor inspector cloth tweaking için tek-struct giriş; tutarlı
default'lar tooling tarafında.

### 74.B — `cd/io/ByteBuffer.hpp`
`std::vector<std::byte>` ince wrapper:
- `append(span<byte>)` bulk copy.
- `append_pod<T>(value)` — trivially-copyable POD bytes ekle.
- `data` / `size` / `as_span` / `release()`.

BinaryWriter (schema'lı) ile yan yana: ByteBuffer hafif no-schema
concat builder, packet assemble + cooker temp buffer için.

## Test

- SoftBodyParams: DefaultsAreSane, ClothPresetSofterThanRope,
  IterationsConfigurable — 3 case.
- ByteBuffer: EmptyByDefault, AppendPodAccumulates, AppendSpanCopiesBytes,
  ReleaseTransfersOwnership, ClearEmpties — 5 case.

`ctest --preset ninja-debug` → **58/58 PASS**.

## Sonuçlar

- 58 ctest yeşil. Tag: **v0.99.6**.
- Cloth solver parameter struct production-ready.
- Asset cook + packet build için lightweight scratch builder mevcut.
