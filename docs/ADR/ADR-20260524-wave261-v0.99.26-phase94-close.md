# ADR-20260524 wave261 — v0.99.26 Phase 94 close

## Bağlam

Phase 94, marathon'un 97. tag'i (patch level). Physics spring/distance
joint + UI right-click context menu.

## Karar

### 94.A — `cd/physics/SpringJoint.hpp`
Two-body distance constraint:
- `SpringJoint { body_a, body_b, attach_a (local), attach_b, rest_length,
  stiffness, damping }`.
- `make_rope_link` factory (stiffness 500, damping 5).
- `make_cloth_link` factory (stiffness 80, damping 0.5).

SoftBodyParams (Phase 73) bulk soft-body; SpringJoint per-pair joint
solver-input. Rope, cloth strand, suspension, anchor.

### 94.B — `cd/ui/ContextMenu.hpp`
Right-click flat popup:
- `add_item(label, action, enabled=true)`.
- `open(x, y)` / `close()` / `is_open()`.
- `invoke(index)` — fires action + auto-close; disabled item returns
  false.
- `clear_items` / `items()` view.

MenuBar (Phase 83) top-bar nested; ContextMenu transient popup at
cursor position.

## Test

- SpringJoint: DefaultStiffnessSane, RopeFactoryStifferThanCloth,
  BodyIndicesStored — 3 case.
- ContextMenu: NotOpenByDefault, OpenSetsPositionAndFlag,
  InvokeFiresAndCloses, DisabledItemNotInvoked, OutOfRangeIndexFails — 5 case.

`ctest --preset ninja-debug` → **58/58 PASS**.

## Sonuçlar

- 58 ctest yeşil. Tag: **v0.99.26**.
- Physics joint family extension (point-to-point distance constraint).
- Editor right-click context UI için backing state.
