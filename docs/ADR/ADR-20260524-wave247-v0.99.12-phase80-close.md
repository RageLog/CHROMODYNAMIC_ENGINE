# ADR-20260524 wave247 — v0.99.12 Phase 80 close

## Bağlam

Phase 80, marathon'un 83. tag'i (patch level). Mouse drag state
machine + cross-range numeric remap.

## Karar

### 80.A — `cd/input/MouseDragState.hpp`
Press → drag (threshold) → release state machine:
- `on_press(x, y)` — origin capture, dragging=false.
- `on_move(x, y)` — distance ≥ threshold ise dragging=true.
- `on_release()` — reset.
- `drag_delta_x/y()`, `press_origin_x/y()`, `is_pressed/dragging`.

Default threshold 4 px — jittery clicks drag olarak yorumlanmasın.
Inspector pan / 3D viewport orbit / asset palette drag-drop için.

### 80.B — `cd/math/RangeMap.hpp`
- `remap(x, in_min, in_max, out_min, out_max)` — linear, extrapolation
  OK.
- `clamped_remap(x, ...)` — input clamped to `[in_min, in_max]`.
- `in_min == in_max` → `out_min` (degenerate fallback).

UI slider → value, audio dB → gain, normalized → pixel rect için.

## Test

- MouseDragState: NotPressedByDefault, PressMoveBelowThresholdNotDragging,
  PressMoveAboveThresholdIsDragging, ReleaseResetsState — 4 case.
- RangeMap: MidpointMapsToMid, ExtrapolatesOutsideRange,
  ClampedRemapStaysInOutputRange, ZeroWidthInputReturnsOutMin,
  InvertedOutputRange — 5 case.

`ctest --preset ninja-debug` → **58/58 PASS**.

## Sonuçlar

- 58 ctest yeşil. Tag: **v0.99.12**.
- Editor mouse interaction primitive katmanı tamamlanıyor.
- Math remap helper sık-kullanılan idiom tek noktada.
