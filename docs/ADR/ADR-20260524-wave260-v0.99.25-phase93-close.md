# ADR-20260524 wave260 — v0.99.25 Phase 93 close

## Bağlam

Phase 93, marathon'un 96. tag'i (patch level). Net token-bucket rate
limiter + ECS generation helpers.

## Karar

### 93.A — `cd/net/Throttle.hpp`
Classic token bucket:
- ctor `(capacity, rate_per_sec)` — full starting bucket.
- `try_consume(n)` — false if not enough tokens (no partial drain).
- `update(dt)` — regenerate; clamps at capacity.
- `tokens()` / `capacity()` / `rate_per_sec()` / `reset()`.

Outbound packet rate cap, anti-spam, API ceiling için.

### 93.B — `cd/ecs/Generation.hpp`
Free-function predicates over Entity:
- `is_null(e)` — generation == 0.
- `same_generation(a, b)` — id + generation match.
- `stale(handle, current_gen)` — handle.generation != current.

Editor cached handle validity, undo replay sanity check için.

### Test fix
Throttle.try_consume `[[nodiscard]]` returnünü silent ignore eden
test satırları `-Werror` ile patladı; `(void)` cast eklendi.

## Test

- Throttle: StartsFull, ConsumeDecreasesTokens, ConsumeOverBudgetFails,
  UpdateRegeneratesTokens, UpdateClampsToCapacity — 5 case.
- Generation: IsNullForDefaultEntity, IsNotNullAfterGenerationSet,
  SameGenerationMatchesId, StaleDetectsOldHandle — 4 case.

`ctest --preset ninja-debug` → **58/58 PASS**.

## Sonuçlar

- 58 ctest yeşil. Tag: **v0.99.25**.
- Net traffic governance için temel rate limiter.
- Entity handle freshness API niyet-net.
