# ADR-20260524 wave242 — v0.99.7 Phase 75 close

## Bağlam

Phase 75, marathon'un 78. tag'i (patch level). Sequence-number wrap
comparison + compile-time query signature primitive'leri.

## Karar

### 75.A — `cd/net/SequenceId.hpp`
Glenn Fiedler's standard wrap-aware sequence comparison:
- `seq_greater_than<T>(a, b)` — half-space distance test ile "a daha
  yeni mi?" sorusu doğru cevaplanır.
- `seq_distance<T>(a, b)` — modulo subtraction; wrap-around aware
  signed gap.

UDP packet sequence ordering (Phase 40 SequenceWindow ile birlikte)
ve any uint16/uint32 monotonic counter wrap için.

### 75.B — `cd/ecs/QuerySig.hpp`
- `query_signature<A, B, C>()` — XOR of `fnv1a_64(typeid(T).name())`
  per component → 64-bit set hash. Order-independent.
- Tooling label / debugging için (overlap detection değil — XOR set
  hash overlap'a cevap veremez, archetype Bitset gerekir).

### Test fix
GTest macro EXPECT_EQ template instantiation içindeki virgülleri
argument separator olarak yorumladı; temp variable pattern ile
çözüldü (test-side cleanup).

## Test

- SequenceId: NewerLessThanOlderViaWrap, BasicMonotonic,
  DistanceWrapsModulo — 3 case.
- QuerySig: OrderIndependent, DifferentSetsHaveDifferentSig,
  EmptyIsZero, SingleComponentNonZero — 4 case.

`ctest --preset ninja-debug` → **58/58 PASS**.

## Sonuçlar

- 58 ctest yeşil. Tag: **v0.99.7**.
- UDP sequence comparison correctness pekiştirildi.
- ECS tooling layer için system-touches-X label primitive.
