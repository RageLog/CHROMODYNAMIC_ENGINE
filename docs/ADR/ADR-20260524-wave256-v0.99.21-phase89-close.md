# ADR-20260524 wave256 — v0.99.21 Phase 89 close

## Bağlam

Phase 89, marathon'un 92. tag'i. Editor prefs key-value store + hex
codec.

## Karar

### 89.A — `cd/editor/PreferencesStore.hpp`
`std::variant<bool, int64, double, string>` value, `string` key:
- `set(key, value)`, `has`, `remove`, `clear`, `size`.
- Type-specific getters: `get_bool/int/double/string` → `optional<T>`,
  type mismatch nullopt döner.

Editor font_size, theme, last_scene, recent_files prefs için.
JSON / binary disk persist caller'a bırakıldı (S2.6 BinaryStream'a
plug edilebilir).

### 89.B — `cd/io/Hex.hpp`
- `to_hex(span<byte>)` → lowercase hex string.
- `from_hex(text, out)` → bool, malformed (odd length, invalid char)
  rejection; both cases accepted.

Asset hash display, packet trace dump, debug pretty-print.

## Test

- PreferencesStore: SetAndGetBool, IntAndDoubleAndString,
  TypeMismatchReturnsNullopt, RemoveAndClear — 4 case.
- Hex: EncodeBytes ({0xAB,0xCD,0x01} → "abcd01"), EmptyInput, RoundTrip,
  RejectsOddLength, RejectsInvalidChar, AcceptsUppercase — 6 case.

`ctest --preset ninja-debug` → **58/58 PASS**.

## Sonuçlar

- 58 ctest yeşil. Tag: **v0.99.21**.
- Editor prefs için tip-güvenli backing.
- Hex codec asset diag/dump path için tek noktada.
