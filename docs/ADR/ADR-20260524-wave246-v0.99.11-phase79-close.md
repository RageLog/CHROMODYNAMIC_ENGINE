# ADR-20260524 wave246 — v0.99.11 Phase 79 close

## Bağlam

Phase 79, marathon'un 82. tag'i (patch level). Scene 3D polyline +
concurrency RAII task group.

## Karar

### 79.A — `cd/scene/Polyline3D.hpp`
Ordered Vec3f points + cumulative arc length:
- `add(p)` — append + length güncellenir.
- `point_at(s)` — arc-length sample, segment-walk + clamp.
- `length()` / `point_count()` / `points()` / `clear`.

AI nav path, debug trajectory overlay, CameraPath bake artifact.
CubicBezier (Phase 22) ve CatmullRomSpline (Phase 26) ile birlikte
3D curve ailesi tamamlanıyor.

### 79.B — `cd/concurrency/TaskGroup.hpp`
RAII fork-join helper:
- `run(fn)` — std::thread spawn.
- `wait()` — bütün thread'leri join + listeyi temizle.
- Dtor `wait()` çağırır (idempotent).

ParallelFor (Phase 54) sayısı-bilinen chunk; TaskGroup ad-hoc
spawn-N pattern. ThreadPool hot-path; TaskGroup setup-free
short-lived job collection.

## Test

- Polyline3D: EmptyHasZeroLength, LengthOfStraightSegment (3-4-5),
  PointAtMidIsHalf, PointAtClampsAtEndpoints, ThreeSegmentArcLength — 5 case.
- TaskGroup: AllTasksRunBeforeDestructor, WaitJoinsExplicitly,
  WaitIsIdempotent — 3 case.

`ctest --preset ninja-debug` → **58/58 PASS**.

## Sonuçlar

- 58 ctest yeşil. Tag: **v0.99.11**.
- AI navigation + debug overlay path için canlı primitive.
- Lightweight ad-hoc concurrency için fork-join idiom yerleşti.
