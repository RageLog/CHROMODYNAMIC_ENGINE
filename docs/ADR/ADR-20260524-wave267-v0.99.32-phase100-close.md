# ADR-20260524 wave267 — v0.99.32 Phase 100 close — **CENTENNIAL PHASE**

## Bağlam

Phase 100, marathon'un **100. tasarım fazı**. Phase 13 baseline'dan
beri 88 phase (13-100, eksik 12 = 13'ten başlıyoruz). Toplam tag:
103. Centennial primitive çifti: scene entity-tag bucket + foundation
counter table.

## Karar

### 100.A — `cd/scene/TagBucket.hpp`
Entity-tag → entity ID set:
- `tag(entity, "Enemy")` (idempotent).
- `untag(entity, "Enemy")`.
- `entities_with("Enemy")` — vector<Entity> span.
- `bucket_count()` / `clear()`.

AssetTag (Phase 35) asset için; TagBucket entity için. "Tüm
düşmanları bul", "tüm UI overlay'lerini bul" gibi O(1) query.

### 100.B — `cd/core/CounterTable.hpp`
String-keyed integer counter:
- `increment(name, delta=1)`.
- `set(name, value)`.
- `get(name)` — 0 if missing.
- `snapshot()` — `vector<(name, value)>` for editor inspector.
- `reset()` / `reset_all_to_zero()`.

ProfileSpan (Phase 52) timing; CounterTable event counting.
"draws_culled", "entities_alive", "particle_count" gibi telemetri.

## Centennial milestone

- **Phase 100** ulaşıldı (Phase 13 baseline + 87 phase iteration).
- **103 tag** ulaşıldı.
- **~270 ADR wave entry**.
- **~170 header-only primitive** mevcut.
- Marathon devam ediyor — v1.0 hala user-only kapısı.

## Test

- TagBucket: TagAddsToBucket, DuplicateTagNoOp, UntagRemoves,
  UnknownTagReturnsEmpty — 4 case.
- CounterTable: IncrementAccumulates, SetReplaces, UnknownKeyZero,
  SnapshotIncludesAll, ResetAllToZeroPreservesKeys — 5 case.

`ctest --preset ninja-debug` → **58/58 PASS**.

## Sonuçlar

- 58 ctest yeşil. Tag: **v0.99.32**. **Centennial phase**.
- Entity-tag query path artık tek primitive.
- Engine telemetri için event counter mevcut.
