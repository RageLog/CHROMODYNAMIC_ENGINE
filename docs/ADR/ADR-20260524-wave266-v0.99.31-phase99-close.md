# ADR-20260524 wave266 — v0.99.31 Phase 99 close

## Bağlam

Phase 99, marathon'un 102. tag'i. Cursor mode state + CRTP singleton
helper.

## Karar

### 99.A — `cd/input/Cursor.hpp`
- `CursorMode { Normal, Hidden, Locked }`.
- `CursorState` — requested mode + applied mode (platform layer
  bridges async), last absolute position + delta tracking.
- `request(mode)` / `mark_applied(mode)`.
- `set_absolute(x, y)` — computes delta from previous.
- `clear_delta()`.

FPS / orbit camera mode (locked + delta), UI hover (hidden), editor
(normal).

### 99.B — `cd/core/Singleton.hpp`
CRTP `Singleton<Derived>` — thread-safe `static Derived` accessor
via `instance()`. Non-copyable, non-movable. Friend access to
private ctor.

CLAUDE.md §7 explicit-injection prefers context/registry; Singleton
escape hatch for process-wide invariant resources (ProfileRegistry,
log sinks).

## Test

- Cursor: DefaultNormal, RequestSetsRequestedNotApplied,
  SetAbsoluteComputesDelta, ClearDeltaZeroes — 4 case.
- Singleton: InstanceIsSame (pointer equality across calls),
  MutationPersists — 2 case.

`ctest --preset ninja-debug` → **58/58 PASS**.

## Sonuçlar

- 58 ctest yeşil. Tag: **v0.99.31**.
- FPS / orbit camera lock UX için cursor state pipeline.
- Singleton escape hatch resmi olarak elde mevcut.
