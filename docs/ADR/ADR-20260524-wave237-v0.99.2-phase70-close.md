# ADR-20260524 wave237 — v0.99.2 Phase 70 close

## Bağlam

Phase 70, marathon'un 73. tag'i (patch level). Anim per-bone weight
mask + scene named layer registry.

## Karar

### 70.A — `cd/anim/BoneMask.hpp`
- `BoneMask(N)` — N float weights, default 1.0 (full).
- `set_weight(i, w)` — `[0, 1]` clamp.
- `weight(i)` — out-of-range read 1.0 (uninit fully-active convention).
- `fill(w)` global set, `resize(N)`.

Locomotion (full body) + aim/reload (upper body) gibi partial-body
layer için. BlendTree2 / AdditiveBlend (Phase 19/31) ile birlikte
kullanılır.

### 70.B — `cd/scene/Layer.hpp`
- `LayerRegistry.add(name)` → `LayerIndex` (uint8).
- `find(name)` / `name(idx)` / `clear` / `size`.
- Iteration order = insertion (deterministic draw layer ordering).

Visibility mask (Phase 38) ile katmanlı: layer coarse grouping
(Background/Geometry/UI), mask fine-grained channel.

## Test

- BoneMask: DefaultWeightIsFullAfterResize, SetWeightClamps,
  OutOfRangeReadReturnsFullWeight, FillSetsAllWeights — 4 case.
- LayerRegistry: AddReturnsIncrementingIndex, FindByNameReturnsIndex,
  NameLookupReverse, ClearEmpties — 4 case.

`ctest --preset ninja-debug` → **58/58 PASS**.

## Sonuçlar

- 58 ctest yeşil. Tag: **v0.99.2**.
- Animation pipeline partial-body blends için layer mask hazır.
- Scene layer ordering (UI overlay, debug overlay, vb.) primitive
  düzeyde sabit.
