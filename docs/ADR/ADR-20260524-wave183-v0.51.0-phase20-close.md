# ADR — v0.51.0 / Wave 183 — Phase 20 close

- Date: 2026-05-24
- Status: Accepted
- Wave: 183
- Tag: v0.51.0

## Sub-phases shipped

### 20.B — `cd::physics::Aabb` + overlap / merge / contains
Header-only AABB primitive. `overlaps(a, b)` /
`contains(a, point)` / `merge(a, b)` / `center` / `extent`.
Foundation for any broad-phase before the Jolt integration
wave lands. 5 new tests.

### 20.E — `cd::concurrency::Stopwatch`
Header-only steady_clock wrapper standardizing the
"restart / elapsed / elapsed_ms / elapsed_us" pattern that
samples + cd::bench + cd::profile had each been re-rolling.
2 new tests.

### 20.A / 20.C / 20.D / 20.F — deferred to Phase 21

AMD CI workflow, editor silhouette outline, Frustum
regression tests, hello_particles sample. Cycle to next phase
to keep marathon discipline (ship coherent primitives, not
half-finished items).

## Version

0.50.0 → 0.51.0 single bump at phase close.

## Cumulative

23 minor releases (v0.30 → v0.51). cd_test_physics +5,
cd_test_concurrency +2.

## References

- ADR-20260524-wave182-phase20-plan.md
- engine/world/physics/include/cd/physics/Aabb.hpp
- engine/foundation/concurrency/include/cd/concurrency/Stopwatch.hpp
