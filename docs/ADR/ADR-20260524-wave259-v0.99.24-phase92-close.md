# ADR-20260524 wave259 — v0.99.24 Phase 92 close

## Bağlam

Phase 92, marathon'un 95. tag'i. Skeletal weapon-attach socket + draw
instancing batch key.

## Karar

### 92.A — `cd/anim/BoneSocket.hpp`
- `BoneSocket { name, hash, bone_index, local_offset }`.
- `BoneSocketSet.add(name, bone_index, offset)` / `find(name)` /
  `all()` / `clear`.
- FNV-1a 32-bit hash cached for fast lookup.

Skeletal mesh weapon attach, particle spawner socket, decorator
mesh pin (helmet, cape) için.

### 92.B — `cd/render/DrawBatchKey.hpp`
24-byte instancing batch key:
- `mesh_handle` (u64), `material_id` (u32), `descriptor_set` (u32),
  `pso_hash` (u64).
- `hash()` — FNV-1a 64 over raw struct memory.
- `operator==` defaulted.
- `static_assert(sizeof == 24)`.

Renderer'ın draw call'ları bucket'lara böldüğü key. Aynı key'li
draws `vkCmdDrawIndexedIndirect` ile tek-draw call.

## Test

- BoneSocket: AddAndFindByName, MissingNameReturnsNull, ClearEmpties — 3 case.
- DrawBatchKey: SizeIs24Bytes, EqualityIsFieldwise, HashStableAcrossEquals,
  DifferentMeshDifferentHash — 4 case.

`ctest --preset ninja-debug` → **58/58 PASS**.

## Sonuçlar

- 58 ctest yeşil. Tag: **v0.99.24**.
- Skeletal mesh attach pipeline'ı için ortak socket veri tipi.
- Draw call instancing yolu için batch key kontratı.
