# ADR-20260524 wave262 — v0.99.27 Phase 95 close

## Bağlam

Phase 95, marathon'un 98. tag'i. Audio voice state machine + atomic
boolean flag with intent-named API.

## Karar

### 95.A — `cd/audio/Voice.hpp`
- `VoiceState` enum: Idle, Playing, Paused, Finished.
- `Voice` mini state machine: play/pause/resume/stop/advance.
- `set_gain` / `set_pitch`, getter'lar.
- Loop = true → end'de wrap; false → Finished.

Mixer (Phase 55) bu Voice array'inden mix yapacak.

### 95.B — `cd/concurrency/Flag.hpp`
- `Flag` over `std::atomic<bool>`.
- `raise()` / `lower()` / `is_raised()` — intent-named over store/load.
- `try_raise()` / `try_lower()` — exchange-and-test.
- `wait_until_raised()` — Backoff (Phase 40) ile poll.

`"is shutdown requested?" → flag.is_raised()` çağrı sitesi
`atomic<bool>.load()`'tan daha okur-okunabilir.

## Test

- Voice: IdleByDefault, PlayThenAdvanceToFinish, LoopWrapsReadPos,
  PauseResumeRoundTrip, StopResetsToIdle — 5 case.
- Flag: InitialFalse, InitialTrue, RaiseLowerToggle, TryLowerReturnsPrev,
  WaitUnblocksFromOtherThread (cross-thread sleep + raise) — 5 case.

`ctest --preset ninja-debug` → **58/58 PASS**.

## Sonuçlar

- 58 ctest yeşil. Tag: **v0.99.27**. **98. release**. 100-tag
  milestone'a 2 release uzak.
- Audio mixer voice contract finalize.
- Intent-named atomic boolean idiom kuruldu.
