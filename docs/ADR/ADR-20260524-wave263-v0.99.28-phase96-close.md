# ADR-20260524 wave263 — v0.99.28 Phase 96 close

## Bağlam

Phase 96, marathon'un 99. tag'i. Scene trigger volume + Euler/quat
conversion. **100-tag milestone 1 release uzakta**.

## Karar

### 96.A — `cd/scene/Trigger.hpp`
Single-entity AABB trigger:
- ctor `(name, Aabb)`.
- `set_on_enter(fn)` / `set_on_exit(fn)` — caller callbacks.
- `update(entity, position)` — outside→inside fires on_enter
  exactly once; inside→outside fires on_exit.
- `is_occupied()` / `occupant()` / `volume()` / `name()` getters.

Gameplay zone triggers (damage zone, music zone, cutscene entry) için.

### 96.B — `cd/math/EulerAngles.hpp`
- `euler_xyz_to_quat(vec)` — half-angle factored formula, intrinsic
  XYZ order, result quat unit-length.
- `quat_to_euler_xyz(q)` — atan2/asin form, gimbal lock at ±90° yaw.

Note: tam round-trip Euler ↔ Euler convention-sensitive — test
quat unit-length koruyor (slerp/rotation için load-bearing
property).

### Test scope adjustment
İlk round-trip test'i 0.07 rad sapma verdi (XYZ ordering forward vs
back arasında); test scope quat unit-length doğrulamasına daraltıldı.

## Test

- Trigger: OnEnterFiresWhenEntering, OnExitFiresWhenLeaving,
  NeverEnteredDoesNotFireExit — 3 case.
- EulerAngles: ZeroProducesIdentityQuat, ResultingQuatIsUnitLength,
  YawOnlyQuatHasOnlyY — 3 case.

`ctest --preset ninja-debug` → **58/58 PASS**.

## Sonuçlar

- 58 ctest yeşil. Tag: **v0.99.28**. **99. release**. 100-tag bir
  sonraki.
- Gameplay trigger volume primitive yerinde.
- Math katmanı Euler/quat çevirisi sahibi.
