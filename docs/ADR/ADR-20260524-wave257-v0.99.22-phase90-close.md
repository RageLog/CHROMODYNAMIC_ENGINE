# ADR-20260524 wave257 — v0.99.22 Phase 90 close

## Bağlam

Phase 90, marathon'un 93. tag'i (patch level). Scene skybox struct +
audio pitch-shift DSP node.

## Karar

### 90.A — `cd/scene/Skybox.hpp`
- `Skybox { cubemap (AssetId), tint, intensity, rotation_y }`.
- `has_cubemap(s)` predicate.

`EnvironmentLight` (Phase 48) lighting kontrib eder; `Skybox` görsel
sky dome. Aynı sahnede farklı cubemap'lere referans verebilirler
(rendered sky vs radiance probe).

### 90.B — `cd/audio/PitchShift.hpp`
Resample-based pitch shift:
- `step = 2^(semitones/12)` — semitone → playback rate.
- Linear interp between neighbor samples.
- `process()` advances read_pos by step; sources wrap at end.

SFX pitch knob kullanımı için. Real-time-quality phase vocoder /
duration-preserving pitch ileride.

## Test

- Skybox: DefaultHasNoCubemap, HasCubemapAfterAssign,
  TintAndIntensityIndependent — 3 case.
- PitchShift: ZeroSemitonesIsUnitStep, OctaveUpDoublesStep (12 st
  → step 2.0), OctaveDownHalvesStep (-12 → 0.5), EmptySourceReturnsZero — 4 case.

`ctest --preset ninja-debug` → **58/58 PASS**.

## Sonuçlar

- 58 ctest yeşil. Tag: **v0.99.22**.
- Renderer skybox kontratı yerinde.
- SFX pitch tooling tek primitive üzerinden.
