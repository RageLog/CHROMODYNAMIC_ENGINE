# CHROMODYNAMIC Engine — Per-Module Completion-% Baseline Report

> Honest, evidence-based completion baseline synthesized from 7 cluster assessments. "% complete" = realistic implementation depth vs. the lib's own stated scope, weighing real impl, test coverage, and explicit stubs/TODOs — NOT LOC alone. The RHI backend is reflected at **100%** (parity mega-marathon phases 1215–1225 just completed; the source JSON's 97% predates that close-out).

---

## Tier Legend

| Tier | Meaning |
|---|---|
| **production** | Real, deep implementation + dedicated gtest binary; ships as engine-runtime code. Stubs/TODOs (if any) are comments or documented v1 limits, not silent gaps. |
| **partial** | Real core logic + tests, but a meaningful sub-feature is deferred/gated (GPU path, second backend, deeper algorithm) — works for its stated scope, not its full ambition. |
| **skeleton** | Orchestration/structure real and tested, but the load-bearing payload (decode/render/inference) is an explicit placeholder. Looks alive, isn't doing the real work end-to-end. |
| **stub** | Bookkeeping present but the core value (e.g. GPU timing) is an explicit placeholder awaiting a dependency. |
| **demo-only** | Minimal reference algorithm + one shader string; missing kernels/wiring claimed in docs. Thinnest assert density. |
| **tooling** | Complete dev/test infra (bench, image-diff, ImGui backend, noise baker) — not engine-runtime; high % means "good tool," not "shippable engine feature." |

---

## 1. engine/foundation

| Lib | % | Tier | Basis |
|---|---|---|---|
| cd::frame_timing | 100 | production | 217-LOC header ring of per-frame dt + mean/median(even-N avg)/p99/min/max + fps_mean/median + percentile(clamped) + jitter + last_dt + copy_in_order(wrap-aware); 34 gtests covering all edge paths (empty ring, wrap-around oldest-slot, even/odd median, percentile clamp, overwrite, reset); const-correct, [[nodiscard]], no markers — genuinely complete (row %/LOC/test-count was stale) |
| cd::bench | 100 | tooling | 312-LOC header microbench (warmup/percentiles/CSV/JSON/MD + DoNotOptimize); print() now shows max+stddev; 24 gtests (12 edge/negative added: warmup=0, single-sample percentiles, name/label survival, slow-body inner_calls=1, CSV column positions, empty-label CSV, markdown pipe count, empty/single JSON array, do_not_optimize non-trivial type, p95 ordering, even-n median); dev-tool |
| cd::concurrency | 100 | production | 3.7k LOC/26 hdrs; real Chase-Lev work-stealing deque + hazard-ptr reclaim, thread pool (598), JobGraph, CoroTask; no open TODO/placeholder ("v1" notes are completed-evolution history). 158 gtests (+34 deepening: WSD single-element owner-pop-vs-steal boundary + empty-steal storm + pow2-cap; HazardDomain<1> single-slot protect/retire + 32-batch auto-scan + slot recycle/concurrent-distinct + try_reclaim no-op; ThreadPool single-worker priority order + shutdown-with-pending drain; WSP shutdown-with-backlog counter consistency + submit-after-shutdown drop; JobGraph failed_nodes under throwing bodies + clear-reset + single-node + diamond-on-WSP + exactly-once-under-WSP stress; Channel MPSC sum + closed try_send + close-unblocks-send + size; EventBus multi-sub order + unsubscribe-miss + clear; Future many-waiters + try_get transition; JobToken complete-unblocks + double-cancel; DeterministicExecutor pump_until cap; RingBuffer + DataChannel wrap-around FIFO; Flag try_raise + cross-thread wait; Once concurrent-once; SpinLock cross-thread try_lock) |
| cd::math | 100 | production | 2.6k LOC/31 hdrs; constexpr Vec/Mat (4×4 inverse), Quat slerp/log, Transform, Onb, splines, noise; +try_inverse (Result-variant) + noise3d (trilinear) + 40 edge/constexpr tests (180 gtests). No existing-formula change (golden-safe). |
| cd::core | 100 | production | 4.7k LOC hdrs (Handle/SmallVector/PoolAllocator/Result/CVar + 2215-LOC portability shim); 2 thin .cpp. SmallVector "insert/erase land later" gap CLOSED (insert/erase/reserve/front/back implemented). **3 real bugs fixed (flagged): (1) SmallVector move-ctor/assign buffer-overflow when source spilled to heap — now pointer-steals the heap buffer; (2) RingBuffer push(const T&) called undefined `emplace_` — latent compile-break for any lvalue push, now `emplace`; (3) CVar::set re-read vars_ outside the lock — data race vs concurrent set/erase, now passes a value snapshot taken under lock. Only test-side consumers depended on the fixed paths (no production callsite regressed).** 178 gtests (+60 edge/negative/boundary/lifetime in test_core_edge.cpp: SmallVector spill-boundary/instance-counted lifetime/move-from-heap/insert-erase/self-assign/move-only, PoolAllocator LIFO-reuse/exhaust-refill/null-dealloc, Handle field-isolation/reset, HandleStore parity/holes/out-of-range, Result and_then/or_else/value_or chains + move-only payload, CVar type-mismatch/retype/snapshot, RingBuffer lvalue-push/lifetime, Bitset word-boundary/cross-word, FixedString exact-fit/clear, Bytes/BitOps/StringSplit/RetryPolicy/EnumFlags/Ref/ScopeGuard/CounterTable boundaries). |
| cd::io | 100 | production | 8 hdrs (BinaryStream/BitStream/Endian/Crc32/Framing/ByteBuffer/Hex/PathUtils); 102 gtests (+60 depth-100 pass: BinaryReader peek/seek-zero/truncated-string-body/partial-read-bytes/write-span/clear-reuse/reserve, BitStream boundary-straddle/zero-bits/seek-past-end/clear-reuse, Endian BE round-trip u16/u32/u64/float/double + store_be/load_be + LE u16/u64 + wire-layout, Crc32 single-byte-vector/per-byte-incremental/two-resets, Framing truncated-header/truncated-body/errored-state-blocks/reset-then-decode/empty-feed, ByteBuffer grow-beyond-reserve/as_span/mutable-data/empty-span, Hex mixed-case/single-byte/invalid-in-middle/all-bytes, PathUtils trailing-sep/extension-empty/stem-hidden/..-at-root/normalize-double-sep/backslash/dot/dotdot/trailing-sep/empty/single-dot/rooted/join-basic/trailing-sep/leading-sep/both-seps/empty-a/empty-b/normalizes-result); new helpers: PathUtils::normalize() + join(), Endian::to_big/from_big/store_be/load_be, BinaryReader::peek(). |
| cd::log | 100 | production | 1k LOC/10 hdrs (Console/Json loggers, RingBufferSink, AuditTrail, Format, PanicDump); 64 gtests (+40 depth-100 pass: Format excess/null-ptr/enum/stray-brace/mixed-escape, JsonLogger newline+tab+cr+control+backspace+formfeed+empty+ts_us+set_level+observer+null-stream, RingBufferSink 1-slot+multi-wrap+clear-invariant+level-preserved, AuditTrail single+exact-cap+ordering+consecutive-clear+cap-0, ConsoleLogger Off+set_level+flush+null-obs+remove-non-added+fan-out+empty+boundary, LogLevel all-distinct+off-strings, Service all-macros-noop+available-false-after-clear, PanicDump null-sink-no-crash) |
| cd::time | 100 | production | 787 hdr + 76 src; HiRes/Steady/SimClock (+set_time()), FramePacer, RateLimiter, TimerQueue (cv-based, no sleep); 47 gtests — SimClock zero-dt/large-delta/scale-0/set_time-jump/set_time-neg-clamp/resume-keeps-scale, FramePacer exact-multiple-alpha-0/reset/set_options/catch-up-drops-residue/leftover==accumulator, RateLimiter set_target_hz/reset, TimerQueue fire-order/past-deadline/stop_token-cancel/monotonic-seq-ids, HiResClock mode+IClock-interface, IClock::now_as<> |
| cd::diag | 100 | production | 362 hdr + 172 src; signal-based CrashReporter (SIGSEGV/ABRT/FPE/SIGINT/SIGTERM), Assert, DeadlineMonitor (arm/disarm/extend_deadline/injected-clock); 41 gtests — Assert file/fn/line capture, CD_ASSERT_MSG, set_panic_handler(nullptr)→default, CrashReporter severity-enum, double-uninstall, destructor-clears, non-fatal no-reporter safe, non-fatal label passthrough, non-fatal after-uninstall noop, SIGINT dispatch, DeadlineMonitor injected-clock no-stall/stall/heartbeat-reset, extend-deadline, disarm/arm, multiple-subsystem selective, is-armed-unknown |
| cd::events | 100 | production | 406-LOC header typed pub/sub EventBus (shared_mutex, ScopedConnection, deferred drain, priority-ordered delivery SEALED); 31 gtests — subscribe/unsubscribe/emit ordering, ScopedConnection RAII+move-construct+move-assign+double-release+default-construct, unsubscribe-during-emit snapshot safety (self + other), deferred drain FIFO ordering, reentrant publish-during-drain, drain-no-subscriber no-op, subscriber_count total, deferred type isolation, EventRecorder capacity/sequence-after-clear; async SEALED to cd::concurrency::EventBus per ADR-20260616 |
| cd::profile | 100 | production | 564 hdr + 80 src; Scope + Buffer/ChromeTrace/Csv/Stats sinks (real chrome-trace JSON); 27 gtests — BufferSink drain/ring-order/clear-invariant/zero-cap, ChromeTrace empty-JSON/name-escape/tid/no-trailing-comma/nested-order, CsvSink empty-file/failed-writes-zero, Stats zero-sample/single-min-eq-max/percentile-p50-p95/reset-reapply, Scope RAII-normal/RAII-exception-unwind; StatRow.percentile_ns() nearest-rank + sorted-cache; BufferSink.drain() atomic snapshot+clear |
| cd::vfs | 100 | production | 302 hdr + 86 src + PathUtil.hpp; overlay VFS (mount priority layers) + Filesystem/Memory sources; 32 gtests; unmount() + normalize_path (dot/dotdot/backslash/sep) + path-normalised exists/read/resolving_layer/list; mod-override complete |
| cd::config | 100 | production | 192-LOC hdr; CVar binary save/load w/ magic+version+sort + std::expected; 22 gtests; wire-format doc corrected (key-before-tag); all 4 types + extremes + empty + UTF-8 + truncation at every field + dup-key + large-100 + merge-semantics sealed |
| cd::plugin | 100 | production | FileWatcher (618: polling+Win32/inotify/FSEvents native) + Loader (135: RAII dlopen/LoadLibrary + ABI/factory/init gates) real; HotReload deepened — `WatchedHotReloader` fuses IFileWatcher (event-driven dirty flag) + HotReloader (load-new-before-teardown-old rollback) so the README-advertised `mount → poll_and_reload` flow is now real; `make_watched_hot_reloader` factory wires real Loader+polling watcher; 38 gtests (+23 depth-100) — FileWatcher multi-modify/throttle/delete-then-recreate/poll-after-stop/idempotent-stop/rewatch, HotReloader mount-twice-already-mounted/mount-load-failure-unmounted/unmount-fires-before-unload/unmount-not-mounted-noop/poll-after-unmount/remount-fresh/failed-reload-then-recovery, WatchedHotReloader mount-starts-watching/mount-missing-rolls-back/no-change-false/change-triggers-reload/no-pending-no-reload/failed-reload-keeps-old/unmount-clears-pending/hooks-pass-through/factory-wires-real-loader. SEAL: real-DLL paths (kAbiMismatch/kInitFailed/kSymbolMissing on a true exported symbol) need a fixture plugin .dll/.so compiled per-platform — covered structurally via mock load fns + the loader's negative dlopen path; full on-disk plugin fixture deferred to a cross-platform test-DLL build step |

| cd::mem | 100 | production | 689 hdr + 90 src; Linear/Pool/Tracking/Page allocators + Pmr; POSIX munmap-length leak FIXED (per-mapping side-table records the rounded length; munmap(ptr, stored_len) on POSIX, VirtualFree(MEM_RELEASE) on Windows; mapping_length()/live_mapping_count() accessors make the table testable); 57 gtests (+39) — detail align_up/is_power_of_two (incl constexpr), SystemAllocator align-sweep 1/8/64/4096 + zero-align-default + null-dealloc, Linear exact-capacity/zero-size/non-pow2-reject/bytes_remaining/dead-backing, Pool LIFO/exhaust-refill/aligned-blocks/overalign-reject/tiny-bump/zero-count-inert/non-pow2-inert/null-dealloc/bytes, TypedPool exhaust + dtor-once, Page map/unmap/size-roundtrip/double-free/foreign/zero-size/over-page-align-reject/sub-page-round-up, Tracking failure-count/zero-size/shared-stats-aggregate/peak-high-water/inner-identity, Pmr throw-on-exhaustion/is_equal-identity-and-foreign/dealloc-forward |
| cd::profile_cpu_marker_overlay | 100 | production | 186 hdr + 259 src; ring Collector + Overlay draw into cd::ui; ~30 gtests (+host-side Collector ring/wrap/overflow + marker nest/depth + per-marker aggregation + overlay row/bar layout math); MarkerSample POD now zero-inits; visual cd::ui draw remains golden-untestable (layout math tested host-side) |
| cd::profile_frame_graph_timeline | 100 | production | 158 hdr + 127 src; Timeline begin/record_pass/end_frame + per-pass GPU ms; ~30 gtests (+begin/record/end ordering, per-pass ms accum + min/max/total, multi/nested pass, frame reset, empty/single, guard paths, overflow boundary) |
| cd::platform | 70 | partial | 1.1k hdr + 1.9k src; Win32Window (489) + X11Window (249) real; web/android/iOS kNotImplemented SEALED; ~43 gtests (4 binaries); +23 depth: OSEventKind/KeyCode/MouseButton enum uniqueness+sentinels, OSEvent/WindowDesc defaults, error-code domain+code round-trips (5 cases), pump_all_windows null-span, FakeWindow multi-event accumulation+drain+idempotent-close, create_window zero-height+both-zero rejection, StableTime non-neg+multi-reset+epoch-bounded, SignalCategory distinct |
| cd::profile_gpu_marker | 70 | stub-sealed | 224 hdr + 180 src; full marker bookkeeping; 1GHz monotonic-counter stub SEALED (deterministic contract); A-QUERY real-path (prepare/resolve via IDevice::get_query_results ns) exercised via FakeTimestampDevice; 21 gtests (+13: exact ms math, nested scope depth+ordering, free-list slot recycling, frame-boundary ring reset, double-end guard, pool-overflow stub fallback, prepare-grow pool replace, MarkerHandle sentinel, multi-frame accumulation, uniform one-tick durations, debug-group name ordering, clear-mid-flight discard, 3-marker slot assignment) |
| cd::foundation_utils | 70 | partial | 38-LOC umbrella; aggregation contract fully verified: 1 test binary, ~35 gtests across 11 suites (compile-time type reachability for all 8 member libs; bench/config/diag/events/frame_timing/plugin/profile/mem functional behaviour; ODR/double-include safety; completeness sentinel); remaining headroom = mem PageAllocator/PoolAllocator/PmrAdapter coverage + plugin HotReload/Loader/FileWatcher functional paths (device-gated) |

**Group rollup: ~80%.** Most mature cluster — ~17/20 production-or-near. Real gaps confined to platform non-desktop backends and gpu_marker's real GPU path (stub sealed, IDevice link wall).

---

## 2. engine/asset/* + runtime + script + texture_synth + imgdiff + imgui_backend

| Lib | % | Tier | Basis |
|---|---|---|---|
| cd::asset (umbrella) | 100 | production | 11 sub-targets (gltf/image/json/ktx2/obj/pak/streaming/cdmesh/cdtex/wav); ~3.5k src incl hand-written JSON (517) + tinygltf (760) + BC7; ~301 gtests (+106 ADD-ONLY defensive-deserialization across 10 sub-targets: json malformed/truncated/depth-limit/unicode-escape, gltf missing-field/bad-accessor, image corrupt/truncated, ktx2 bad-magic/unsupported, obj malformed-face/oob-index, pak truncated-toc/dup-key, wav non-PCM/truncated, cdmesh/cdtex version-skew+truncated; every loader rejects corrupt input via Result, no UB). Valid-input decode byte-identical (golden-safe). BC3/BC5/KTX2-supercompression/glTF-morph/JSON5 sealed future-scope |
| cd::script | 100 | production | 1.3k src; embedded Lua 5.4 (vendored) via PIMPL — run_string/file, globals, register_function, 877-LOC ECS-world bindings; 89 gtests (+48 in test_script_edge + test_lua_bindings_edge: compile/runtime/missing-file Result split, moved-from degradation, instruction-cap on call_global, nil-padding under-returns, re-entrant native callback, (nil,message) contract for dead-entity/missing-component/non-table payload, Vec/Mat operator paths + raises, event unregister-miss + erroring-handler-counted-failed, null-guards); stale "Wave 72" doc markers corrected; no production-logic change (golden-safe) |
| cd::imgdiff | 100 | tooling | header ~955 LOC; real FLIP-lite (Andersson 2020) + SSIM (Wang 2004) + Gaussian, cited; 65 gtests (+33 ADD-ONLY depth-100: allblack-vs-white max-delta, 1×1 identical/different for all paths, highlight null+dim-mismatch, passes-boundary, empty-image error, SSIM null+zero-window+exact-edge+passes-helper, Gaussian null+1×1-clamp+kernel-sum-via-constant+output-dims, FLIP-lite perceptual-map reference-values+null+custom-threshold+heatmap-dim-mismatch+p95-zero+error-map-size, FlipFull null+1×1+p95-invariant+chroma-sigma+error-map-size, SsimGaussian null+zero-stride-clamp+mean-ge-min+small-image+smaller-than-kernel); math BYTE-IDENTICAL; golden-image tool |
| cd::asset::texture_compress | 100 | production | 803 src; BC1 encode/decode/PSNR (min/max endpoints, 4-color + 3-color decode branches, degenerate all-same-color, alpha-ignored) + optional BC7 (bc7enc) + ASTC 4×4/8×8 (ARM astcenc) behind flags; 28 gtests (T1–T19 prior + T20–T28: block-boundary 8×4, zero-dim guard, gradient PSNR>15dB, PSNR-formula monotonicity, decode-known-block, alpha-ignored byte-identical, checker RMSE=0, sub-4 mip-chain 24B, two-grey PSNR>30dB); BC3/BC5 SEALED (encode→nullopt, documented not-in-scope) |
| cd::asset::material_authoring | 100 | production | 322 src; JSON round-trip of AuthoredMaterial; 27 gtests. REAL FEATURE: schema versioning (kCurrentSchemaVersion + write/read + future-version reject + legacy v0 backward-compat) + unknown-field collection + validate checks #8 (schema_version) #9 (unknown fields). Backward-compatible API + JSON |
| cd::texture_synth | 100 | tooling | header (Noise 105 + Earth 156); deterministic procedural noise + Earth baker for fixtures; 36 gtests (+24: noise determinism across calls + hash21 golden lock + seed independence + grid-corner identity + continuity + 6-oct bound, Earth albedo/normal/MR golden pixels + size-independence + unit-normal-encode + zero-size safety). Noise/baker output BYTE-STABLE |
| cd::asset::shader_cache | 70 | partial | 254 src; real binary cache format (CDSC magic/version/timestamps) + serialize/lookup; 21 gtests (T10-T21: bit-identical stream, timestamp field round-trip, empty cache 0-entry, lookup hit/miss, empty-SPIR-V round-trip, duplicate-key overwrite+disk, large-cache 100-entry, truncation mid-entry, merge/collision semantics, entry_count noexcept, spec_const_hash non-zero, empty-string key fields); cache-only by design, no compiler integration — SEALED |
| cd::asset::vfx_authoring | 70 | partial | 351 src; JSON authoring/round-trip for VFX/particle descriptors; 24 gtests (T13-T24: all-preset RT, emitter_kind, NaN rate/life, size neg, per-axis vel inversion, multi-error accumulation, file-not-found, root-array, empty-id, partial-array); runtime simulation tie-in SEALED out-of-scope |
| cd::asset::streamer_pool | 70 | partial | 175 src; priority-weighted round-robin token dispatch (Bresenham deficit); 22 gtests (T12-T22: Bresenham token-sum invariant, equal-weight even split, deficit carry-over, attach/detach mid-dispatch, budget exhaustion, reconfigure ratio change, max_concurrent_loads=1 budget-invariant, exact integer split no residual, three-streamer proportional share, reconfigure on clean pool); governs streamers whose payloads are stubs — SEALED |
| cd::imgui_backend | 70 | tooling | 378 src; real Vulkan ImGui backend (impl_vulkan + win32 input) + ProfilerView; 22 host gtests (FIRST test binary): color_from_name_hash EXACT per-channel pins for Render/Physics/empty-offset-basis + embedded-NUL/one-char-divergence edges; profiler_flamegraph branch detection via ImGui cursor-advance (empty/degenerate reserve no canvas, draw path reserves rows*row_height, repeated thread_hash collapses to one row); Context::create null-guard both sides + diagnostic-message + InitDesc defaults; imgui_errors domain/Code/make contract. GPU draw path (ImGui_ImplVulkan_RenderDrawData) device-gated (renderer + rhi pixel-parity capstone). D3D12 header SEALED — include-facade, no cd:: API to test, RAII wrapper deferred to M4H |
| cd::asset::validator | 70 | partial | 250 src; magic-header sniff (PNG/JPG/KTX2/DDS/HDR/GLB/WAV/OGG) → Severity issues; 31 gtests (+15: each format magic verified, truncated-magic OOB-safe, JSON leading-whitespace, all-whitespace error, GLB v0 warning, exact/over pixel-limit boundary, OGG trailing bytes, WAV-11-byte, policy() accessor default+post-configure, T27 deep-schema SEAL); shallow by design (no deep schema parse — SEALED) |
| cd::runtime | 70 | partial | 55 src + 128 hdr; thin DI aggregator wiring VFS + asset registry + lazy thread pool + logger; 28 gtests (+14: CVar bool/double/string types, missing-key nullopt, overwrite-wins, VFS front-shadows-back priority, VFS not-found negative, lazy worker_count post-init, pool stats observable, watchdog no-stall-on-heartbeat via injected clock, watchdog disarm skips evaluation, context independence, asset load missing-path error, asset load idempotence + cached_count); runtime loop out-of-scope — SEALED |
| cd::asset::texture_streamer | 70 | partial | 430 src; real priority/dedup queue + async pool; .cdtex BC7 decode WIRED (real width/height); PNG/JPG stb path SEALED (ADR-20260616-band6); 27 gtests: priority order (3-item drain), dedup idempotent, cancel-loaded no-op, cancel-unknown no-op, get_dimensions pending nullopt, sealed-ext nullopt, live_texture_count, mip_target propagation, worker_count=0 normalize, block-byte fill verify, large batch (10), pool+streamer count consistent, async dedup, async cancel-before-tick |
| cd::asset::scene_streamer | 70 | partial | 415 src; real queue + AsyncScenePool + cd::asset::gltf::load_scene() wired in BOTH sync and async paths; SceneId allocation/uniqueness/stability; residency lifecycle (enqueue→tick→is_loaded→get_scene real node/mesh tree); cancel/evict; capacity; completion ordering; GPU/ECS ingest is render-side (SEALED Sprint-2 boundary); 33 gtests (T1-T19 sync + A1-A14 async; NO sleep_for) |
| cd::asset::audio_streamer | 70 | partial | 425 src; real queue + AsyncAudioPool + real WAV RIFF/WAVE decode (OGG sealed — no Vorbis loader, Band-6 ADR); 29 gtests (T1-T9 sync lifecycle/priority/cancel/decode/idempotent; T10-T14 post-load idempotency/negative-cancel/between-tick-cancel/equal-priority-drain/format-stability; A1-A7 pool dispatch/accumulate/non-block/format/AssetId/failed-drop; A8-A15 dedup/cancel-before-dispatch/dtor-graceful/partial-tick-drain/sync-noop/mixed-batch/inflight-exhaustive/no-double-count); OGG SEALED |

**Group rollup: ~68%.** Strengths: asset umbrella (real loaders, deeply tested) + script (full Lua) + all three streamers now at 70% (scene_streamer raised from 45%: real glTF parse end-to-end + 26 gtests, audio_streamer real WAV decode + 29 gtests, texture_streamer real BC7 decode + 27 gtests). imgui_backend now has its first host test binary (22 gtests over the device-free surface); its remaining headroom is the GPU draw path, which is device-gated by design.

---

## 3. engine/game

| Lib | % | Tier | Basis |
|---|---|---|---|
| cd::game::quest | 100 | production | 653 src; objective tracker/journal, commit-or-rollback add, auto-complete funnel, bounds-checked serialize; 28 gtests — empty-obj-id rejection, add-rollback, pre-failed-obj activation, find_quest/size/empty, empty-log serialize/restore, mid-payload truncation, corrupt-obj-status-byte, overflow-clamp, out-of-order completion, large-batch round-trip |
| cd::game::save | 100 | production | 1069 src + fwd-compat skip fix (bool/null/nested obj); atomic tmp+rename + meta versioning + migration chain + cloud hook; 47 gtests — 27 prior + 20 new edge/negative (migrate no-op/notfound/invalid/hop-cap/null-fn, cloud-upload-fail, cloud-dl-fail/notfound, clear-handlers, lwm-invalid, slot_exists-invalid, list-nondir/nometa, label-fallback, fwd-bool-null, fwd-nested, stale-tmp, noroot-list, missing-body, version-skew) |
| cd::game::ai_bt | 100 | production | 200 src; Sequence/Selector/Parallel/Inverter/Repeater/UntilSuccess/UntilFailure/ConditionNode + blackboard; Champandard/Colledanchise cited; 38 gtests — empty-tree, threshold boundaries, cross-tick resume, deep nesting, null-child, node_kind/node_children, Blackboard::find/set(Value), decorator reset, UntilFailure all paths, ConditionNode gate |
| cd::game::cutscene_player | 100 | production | 590 src; seek/restart/total_duration_ms added; 40 gtests — all prior + seek mid-phase/to-zero/past-end/negative/idle/paused, restart after-complete/mid/never-loaded, total_duration, zero-duration-phase clamp, events-empty-after-stop, pause/resume idempotent, play-force-reset, JSON missing-cutscene_id/file-not-found/root-array/can_skip-default, seek-clears-buffer |
| cd::game::dialogue | 100 | production | 492 src; branching VM + DSL parser (NODE/TEXT/CHOICE/END) + Condition lambdas + kBrokenLink tolerance; 47 gtests — all prior + 34 new edge/negative (DSL unknown-directive/text-before-node/choice-before-node/node-missing-id/speaker-missing-name/end-with-args/choice-missing-arrow/choice-missing-colon/choice-empty-id, empty-source, comments-only, multi-text-join, CRLF, inline-comment-strip, implicit-end, duplicate-id-via-load_tree, cyclic-loop, deep-10-chain, throwing-condition propagates x2, reset-after-empty-next-node, null-bb-skips-condition, unloaded-queries-empty, start_node_id accessor, reload-replaces, Blackboard fallback/wrong-type/erase-clear/type-erased-set, empty-dialogue-terminal, empty-text-body, choice-empty-prompt, broken-link-preserves-node, node_count-zero) |
| cd::game::input_recorder | 100 | production | 267 src; HL2-style record/replay, CDIR v1 LE binary format + timestamp cursor; README API corrected; 22 gtests — empty recording, single event (all fields), large 1K round-trip, version mismatch, trailing garbage, out-of-order timestamps, seek past-end, pre-start no-op, restart-clears-buffer, fresh-replayer all()/is_finished()/event_count(), save-before-start |
| cd::game::l10n | 100 | production | 500 src; key=value + CLDR plurals (en/tr/ar/ru baked — all 6 categories) + {n} subst + RTL detect; 38 gtests — ar all-6-categories, ru 4-categories, plural_suffix all cats, .other fallback all categories, set/clear direct, has_locale, add_locale-replaces, multi-observer order, self-remove-during-broadcast, malformed-skip, idempotent-load, base-fallback paths, is_rtl comprehensive |
| cd::game::settings | 100 | production | 389 src; INI key=value w/ comment preservation + heuristic typed parser via charconv; +off_change unsubscribe + lossless float round-trip (from_chars + max_digits10) + 30 tests. |
| cd::game::ai_director | 100 | production | 168 src; intensity pacing + encounter-template select + saturating score + saturating multiply overflow fix; 39 gtests/108 asserts — zero/negative/extreme-dt, intensity-min/max saturation, score sub-unit/overflow/boundary-multiply, configure-replace/clear, single-template, tier-gap skip/select, all-same-tier tie-break, kRelief accelerated-decay + no-kBuildUp exit + direct-to-kIdle, natural-decay-to-idle, event-id advisory, EncounterTemplate field round-trip, exact-threshold boundary selection |
| cd::ai::pathfinding | 100 | production | 541 src; A* navmesh (Hart/Nilsson) + Triangulator (Recast/Snook); 30 gtests/97 asserts — has_navmesh state, heuristic-admissibility corridor, determinism, 3-component unreachable, same-tri explored=1, path-reconstruction total_distance, nearest_triangle index; empty-input no-throw, degenerate collinear drop, OOB-vert drop, configure round-trip, radius=0 disables filter, non-manifold edge no-throw, winding-independence |
| cd::game::anim_graph | 100 | production | 323 src; PlayClip + 1D/2D blend-tree tick over cd::anim Pose/Skeleton; 29 gtests (+20 edge: empty/single-frame/zero-duration clip, loop-vs-clamp past end, 1D threshold/out-of-range, 2D corner pins, pose-count-mismatch blend, OOB states) |
| cd::game::asset_hot_reload | 100 | production | 309 src; note-then-flush over FileWatcher + create/delete probe + throttle; 18 gtests/125 asserts — flush-no-pending, multi-dirty-tick, unwatch-rewatch, untracked-no-op, throttle-boundary-exact, deletion-wins-mod-same-window, burst-single-tick-coalesce, set-throttle-runtime, self-unsubscribe-safe, path-count-decrements, invalid-id-no-op |
| cd::game::camera | 100 | production | 314 src; Cinemachine-style CameraBrain priority/blend + VirtualCamera; +[[nodiscard]] on add_vcam/remove_vcam + 22 gtests (priority tie-break, mid-blend interrupt/add/remove/warp, blend curve endpoints, no-active-vcam, dt=0 freeze) |
| cd::game::dialog_tree | 100 | production | 229 src; BG3-style condition-routed graph + kCondition chaining + O(1) hash index; +cyclic-kCondition depth-guard (1024) fix + 10 tests (18 gtests/84 asserts: chained conditions, broken-link, missing-false-branch, cycle, duplicate-id, empty-tree, deep-chain, reload, vars-survive-load) |
| cd::game::fsm | 100 | production | header-only by design (478-LOC Harel statechart: flat+hierarchical + shallow/deep history); .cpp is ABI anchor; 26 gtests/~92 asserts; gaps closed: guard-reject/unhandled-no-op/outer-before-inner/start-idempotent/accessor/shallow-vs-deep-3-level/history-never-visited(shallow+deep)/self-transition-hfsm/null-predicate/set_state-region-exit/orthogonal-regions/entry-exit-ordering |
| cd::game::particles_event | 100 | production | 289 src; recipe-staging burst dispatcher (no GPU coupling by design) + dense-handle tombstone with slot-reuse free-list + BurstHandle generational UID + tick negative-dt fix; 29 gtests — gaps closed: recipe-slot-reuse, double-free-guard, stale-BurstHandle-expiry, kInvalidHandle-lookup, fire_handle-unknown, zero-count-burst, zero-lifetime-burst, tick-zero-dt-noop, tick-negative-dt-noop, capacity-growth-200, max-count-decay, fire-empty-name, recipe-name-echo, replace-in-place-live-burst, callback-self-remove-snapshot-safety, staging-order, dispatch-no-recipes, handle-uniqueness, find-recipe-null-after-unregister |
| cd::game::query | 100 | production | 422 src; ray/AABB slab + frustum over spatial-hash broad phase + dedup, RTR4e/Williams; 30 gtests; gaps closed: slab-behind-origin, grazing-edge, non-unit-dir, max_dist-boundary, frustum-straddle/boundary-touch/empty, 6-plane-overload, dedup-seen-set, dense-same-cell, negative-coords, zero-size-query-AABB, degenerate-entity-AABB, add_entity-replace, rebuild_from-null, box_query-empty, brute-force-fallback |
| cd::game::save_compression | 100 | production | 374 src; custom RLE codec + optional LZ4 block (gated); +7 edge tests (20 gtests: single-byte ratio, 128/129 run boundary, 256 two-packet, all-distinct worst-case, empty benchmark, size-mismatch guard, LZ4 corrupt-guard) |
| cd::game::trigger | 100 | production | 274 src; TriggerWorld enter/stay/exit + despawn-exit + clear_occupancy; brute-force O(V·S) broad phase (query-lib integ SEALED out-of-charter G3.2 — body-only future commit, zero API churn); 24 gtests; gaps closed: re-enter-after-exit, zero-occupants, simultaneous-multi-volume, exact-boundary-inclusive(AABB+Sphere), simultaneous-enter+exit-different-volumes, remove-mid-overlap, clear_occupancy, layer-clamp-geq64, find-null/post-remove, despawn-exit-for-layer-filtered-subject |
| cd::ai::squad | 100 | production | 281 src; CPU squad/formation complete; ~30 gtests (+formation slot offsets line/wedge/column/circle, leader/follower, regroup, member add/remove, slot reassign, cohesion/spacing, empty/single/max). GpuBatchSolver SEALED (Sprint-3 GPU batch solve needs buffer-ownership + dispatch wiring, multi-week). slot_offset convention: even→+, odd→− |

**Group rollup: ~87%.** Most uniform cluster — 19/20 production, every lib has a substantive .cpp, documented format, citations and a dedicated gtest binary. Zero genuine kNotImpl/stub code (all "stub/TODO" hits are comments). Only ai_squad's GPU path is genuinely deferred.

---

## 4. engine/render — CORE (rhi + shader chain + framegraph/material/brdf/camera + render umbrella)

| Lib | % | Tier | Basis |
|---|---|---|---|
| cd::rhi | **100** | production | **24.5k src; Vulkan ~8.2k + D3D12 ~8.6k + Metal ~8.6k all real; IDevice 75 virtuals + ICmdBuf 46; 340 tests incl cross-backend PIXEL-PARITY + readback-parity capstone. Backend parity mega-marathon CLOSED (phases 1215–1225). ⚠ per-file header banners are STALE ("boot-only/50 virtuals stubbed") — don't baseline off comments.** |
| cd::gluon | 100 | production | Shader-MODULE lib: 21 embedded .glsl modules (~1.5k LOC SOTA-cited BRDF/tonemap/IBL/LTC) + 47-LOC resolver; 36 tests (+12 GPU-free registry/resolver-contract: canonicalisation, determinism, find_module boundaries, guard triad, catalogue integrity); no shader-math change (golden-safe); consumed by all 3 RHI backends |
| cd::material | 100 | production | 1035 src + 2.6k hdr (StandardPbr/LitPbr/Skinned/Sky/BrdfLut/RtClosestHit); 66 tests (+15 CPU-contract: alpha clamp/mode, albedo/emissive HDR clamp, release/move lifecycle, inert update guard, AlphaMode predicates, extra_set_layouts multi-set, missing-stage rejection); no BRDF/render change (golden-safe) |
| cd::shader | **100** | production | 756 src; CachedCompiler + real glslang-backed GlslangCompiler + FileWatcher hot-reload; **55 tests** incl live-edit + corrupt-cache self-heal + per-field cache-key sensitivity + closure (nested/unresolved/cyclic) + compute/HLSL/debug-info/all-stages + deterministic forced-mtime watcher edges; corrupt-cache-file EVICT path added (ADD-ONLY, no SPIR-V change). Slang path SEALED out-of-charter (ADR-20260620-shader-slang-gate-seal) — gate-stub retained |
| cd::camera | **100** | production | header-only 803 LOC; Camera/Frustum/FirstPerson/Orbit/CameraPath/Lens with real math bodies; **60 tests** (+31 ADD-ONLY edge/negative: perspective known-value at fov/aspect/near/far extremes + depth-row [0,1] near/far map + zero/neg-aspect unity fallback + tight-near/far finiteness; ortho known-value + frustum extraction in/out; plane-normal unit-length + face-enum order lock + AABB on-near-plane + sphere side-tangent; FirstPerson pitch ±pole clamp + zoom dist_min/max clamp; Orbit elevation pole clamp + radius window clamp; CameraPath sample t<first/t>last clamp + exact-endpoint + midpoint LERP + out-of-order add_key sort + empty-path −Z target; Lens fov↔focal round-trip + neg/zero-focal fallback; look_at degenerate eye==target & up∥forward finiteness; auto_frame zero-extent box + distance-scale; Viewport center/zero-dim/offset-rect round-trip). No view/proj/frustum/lens math touched (GOLDEN byte-identical) |
| cd::spirv_cross_glue | **100** | production | 394 src SPIRV-Cross bridge; GLSL/HLSL/MSL emit all compiled + exception→Result; **16 tests**: all three target happy-paths + explicit version opts (GLSL 330 + default 450, HLSL SM51) + GLSL round-trip recompile + 3× empty/malformed negative (translate + translate_msl cfg overloads) + MSL cfg arg-buf-on/off + compute workgroup 8×4×2 (cfg + flat path) + frag 1×1×1 default + HLSL push_constant→b0/space1 remap; ADD-ONLY, zero emit change (golden-safe) |
| cd::debug_line | 100 | production | header-only 363 LOC; RHI-independent CPU line-batch (AABB/OBB/frustum/circle → kLineList stream); 46 tests (phase1252: +26 edge/negative covering degenerate AABB/OBB/frustum, circle seg clamping 0/neg/2, capacity retention, full RGBA propagation, per-shape vertex counts, polyline 2-pt, arrow ±Y fallback + head_frac clamp, grid neg half_lines, sphere zero-radius + per-axis plane confinement); bgfx/Bevy cited |
| cd::hdr_display | 100 | production | header-only 133 LOC; PQ ST-2084 encode/decode + Rec2020 matrix + scRGB pack/unpack + Rec2020↔XYZ matrix pair, cited constants (ST-2084, BT.2020-2); 17 tests (PQ round-trip full range 0/0.001/1/100/1000/10000 nits, strict monotonicity 1-nit steps, BT.2100 Table 4 reference values, neg/overflow clamp encode+decode, Rec2020↔XYZ inverse round-trip + luminance coefficients, scRGB pack neg/super-white, unpack round-trip 4 cases, SDR identity) |
| cd::brdf | 100 | production | header umbrella aggregating ltc/sheen_clearcoat/sss lobes; ~16 cited fns (Heitz/Estevez-Kulla/Burley); 52 tests (+35 paper-verified: LTC matrix/edge-integral/polygon NaN-guards/energy-bound, Charlie-D + Neubelt reciprocity + clearcoat reference/floor, Burley SSS profile/kernel energy-conservation, CPU↔GLSL parity); no lobe math touched (golden-safe); GPU lobes live in gluon |
| cd::scene_ingest | 100 | production | 275 src; ingest_gltf_scene DFS → GPU upload + ECS entity/transform + AABB accum + rollback; 21 tests (+8: empty/single-node, deep DFS world-compose, AABB pass-through + empty-mesh node, rollback ECS+GPU-clean invariant, OOB-child tolerance, multi-mesh); no ingest-output change (golden-safe) |
| cd::framegraph | 100 | production | real add_pass/compile/execute + per-pass timing + barrier helper + identity-elide + imported final-state transition + opt-in backward-reachability dead-pass cull (golden-safe, default OFF) + Kahn topo-sort host utility w/ cyclic-node diagnostic (`topo_cyclic_nodes`); 34 Null + 5 Vulkan tests (+14 Null: topo empty/single/self-loop/dup-edge/FIFO-determinism/cyclic-node-set/dominated-nodes, cull diamond/multi-level-dead-chain/RMW-promotion, invalid+stale-generation handle, undefined-final-state no-finalize, instrumentation per-pass order); transient-memory aliasing + DAG pass-reorder SEALED per ADR-20260616 (needs frozen Band-0 RHI placed/aliased-resource primitive — out-of-charter; reorder would break golden) |
| cd::debug_draw | 100 | production | 213 src + 143 hdr immediate-mode GPU line-batch renderer (Material pipeline + kLineList VB + frames-in-flight-safe park/reclaim); **28 NullDevice tests + 4 Vulkan integration tests** (32 total): RendererDesc defaults (name/depth-format/GLSL-overrides/color-formats), observer zero-state (vertex_capacity_bytes/parked_buffer_count/is_valid), destroy() idempotent x2, flush()-on-invalid no-op (empty/non-empty/large/multi-frame), triple move chain, create() error path, recreate_pipeline() x5 calls, kDestroyMargin static_assert (==3/>0/>=3), sequential-frame stability. Real impl, no stubs. |
| cd::render (umbrella) | 100 | production | Renderer.cpp 371 + 12 hdr (DrawBucket/SortKey/PostProcessChain/PlanarShadow); 93 tests (+37 ADD-ONLY host: SortKey bit-mask/ordering, DrawBucket FIFO/sort/clear, PostProcessChain compose/toggle/order, PlanarShadow matrix, DrawBatchKey FNV, MeshStats, Tonemap, TextLayout, ClearColor). No render path touched (golden BYTE-IDENTICAL) |
| cd::async_submit | 100 | production | header-only (157+159); real thread+cv worker, single + N-slot render-thread primitive; ~25 tests (+submit/drain order, N-slot wrap/backpressure, shutdown-with-pending drains, submit-after-shutdown rejected, worker exception isolation, double-shutdown). scoped_lock migration; frame-fence integration SEALED (deliberate minimal scope) |
| cd::mesh_shader | 100 | production | header-only Meshlet.hpp ~246 LOC; real greedy clusterizer (64-vert/124-tri caps + slot dedup + flush-on-overflow) + Ritter bounds_sphere + cone_axis_cutoff (Karis Nanite) + new host-mirror `cone_cull()` predicate (byte-identical to the task-shader GLSL backface test, ADD-ONLY); **21 tests** (+8 BAND-4 ADD-ONLY: every-triangle-reconstructs-to-exactly-one-global-triple partition contract over a 40-tri/2-meshlet split, 124-tri cap leg via fixed 64-vert pool of 174 tris, large-mesh meshlet-count bound [9,48] over 1000 tris, Ritter sphere known cube centre+radius=√3/2, all-identical-vertex zero-radius collapse, cone_cull sentinel-never-culls + view-opposes-cone hides + cutoff-width gates band). build_meshlets() output behaviour-preserving (hello_engine meshlet overlay draws from it — GOLDEN-SAFE). cone/spatial fit + GPU dispatch SEALED out-of-charter (ADR-20260616 §2.3, dispatch in rhi) |

**Group rollup: ~84%.** Anchored by cd::rhi at 100% — three genuinely-deep backends with a pixel-parity capstone. No real stub/skeleton libs here; the spread is production-vs-partial, not real-vs-fake. WARNING for the new plan: rhi's stale header banners badly understate completeness.

---

## 5. engine/render — FEATURES tier

| Lib | % | Tier | Basis |
|---|---|---|---|
| cd::restir_di | 100 | production | 1512 hdr + 2649 src/5 TUs; full RHI ReSTIR DI (initial/temporal/spatial reuse CS) + complete SVGF denoiser + orchestrators; 71 tests (+39 ADD-ONLY: WRS reservoir final_weight/update/combine/clamp_history host-math boundaries, TemporalBuffer ping-pong + max-age invalidation, GPU-gated denoiser/SVGF/pipeline edge cases); no ReSTIR/SVGF math change (golden-safe). Sprint-7 G-buffer seam wiring (depth/normal/mesh_id views, motion-vector reproj) SEALED out-of-charter — activating it alters rendered output, needs a dedicated GPU render-review |
| cd::ddgi | 100 | production | 1522 hdr + 1529 src; full RHI 4-pass (trace/blend-irradiance/blend-visibility/sample) + descriptor writes/UAV barriers + ray_query; 79 tests (+47 ADD-ONLY: probe-grid count 0/1/asymmetric + flat_index↔shader-decode round-trip + partial/below-origin world→probe mapping + trilinear edge-clamp + atlas dims/zero-atlas guard/-Z fold UV + dense octahedral round-trip + ambient_fallback branches + TraceSettings defaults [CPU, runs everywhere]; PC/UBO POD sizes + GLSL source contract + CPU-stub execute_* validators/counters [CPU]; GPU-gated set_inv_vp/set_sun_light pre-init + smoke-variant rejection + bind_sample_resources null-view/pre-init rejection + single-probe minimum grid + shutdown idempotence + FullPipeline set-forwarder error channel); no probe/blend/sample math or GLSL change (golden-safe, byte-identical). Deeper GPU-integration seam (wiring DDGI sample into the live hello_engine HDR composite — activating it adds an indirect bounce that alters rendered output) SEALED out-of-charter, needs a dedicated GPU render-review |
| cd::post | 100 | production | ~3.8k LOC/13 hdrs (composite 1147, exposure 532+102 .cpp, bloom 457, taa 214) — tonemap/bloom/TAA/SSR/GTAO/DOF/motion_blur/SMAA/camera; ~163 tests (+79 ADD-ONLY host/NullDevice + new cd_test_post_camera bin: per-effect Settings opt-in-default invariants, kernel-weight/Push-layout/GLSL-contract locks, param clamps, error paths). No effect math/GLSL/default touched (golden BYTE-IDENTICAL); GTAO crude-integrator + exposure GPU-reduce + composite 3D-LUT SEALED (render-altering) |
| cd::lighting_clusters | 100 | production | 344 hdr + 850 src; clip-space sphere binning + AABB-sphere tight test + prefix-sum + glslang cluster-cull CS w/ dispatch; 35 tests (+22 ADD-ONLY host-math, new cd_test_lighting_clusters_hostmath bin [runs everywhere, no device]: cluster_index bijection over whole grid + endpoints + degenerate 1x1x1; prefix-sum empty/single/dense-overlap monotone+closed + slice-reconstructs-packed-stream round-trip; light-count 0/1/many index-range; out-of-frustum reject behind-camera/in-front-of-near/beyond-far + lateral bounded footprint; AABB tight test small-hit + fat-vs-thin spanning + depth-slice straddle; lights_in_cluster out-of-range + undersized-assignment guards; DispatchPass static group_count/buffer-size/std430-POD-layout boundary locks); no binning/cull math or GLSL change (golden BYTE-IDENTICAL). The Sprint-3+ parallel-prefix-sum GPU compaction + framegraph view-proj wiring are documented future-optimisation notes (activating either alters dispatch output) — SEALED out-of-charter; ⚠ overlaps cd::cluster (documented note, NOT merged) |
| cd::ibl | 100 | production | 762 LOC/5 hdrs; CPU bakers — split-sum BRDF LUT/irradiance/prefiltered specular/equirect→cube; 36 tests/112 asserts (+22 ADD-ONLY host regression locks: Karis BRDF-LUT corner + energy bound, Hammersley/vdC/GGX/Smith-G helpers, irradiance linearity + ratio preservation, equirect↔cube cardinal UV + seam continuity + 6-face round-trip, face-index dominant-axis, prefilter mip monotonic + halve-clamp + NaN guards). No baker math/constant/NOLINT-seal touched — chrome IBL bake BYTE-IDENTICAL; GPU side in cd::ibl_gpu |
| cd::cluster | 100 | production | 691 hdr + 506 src; RHI two-pass count/write compute + CPU reference + PBR lookup; ~45 tests (+28 ADD-ONLY: grid dims, finalize/clear lifecycle, sphere in/out/spanning, near/far depth slices, two-pass count↔offset↔index invariant, PBR push-constant + 3-SSBO wire contract, new Vulkan-gated tests/gpu/ bit-for-bit parity vs CPU reference). No compute/froxel/PBR math touched (golden BYTE-IDENTICAL); ⚠ duplicates cd::lighting_clusters froxel assign (documented, not merged) |
| cd::light | 100 | charter-complete | 739 LOC/5 hdrs; charter scope = header-only math primitives (no RHI dispatch). **~83 tests** spanning all 5 families — Attenuation (14: zero-d epsilon guard + custom-epsilon + half-range window pin + exact-at-boundary + monotone + inner/outer boundary exact + smooth-ramp formula + spot analytic + spot-GE-point + point exact + lux identity/neg/zero + range-guard + beyond-range), ColorTemperature (15: all 3 Krystek polynomial branches + both clamp endpoints + non-negative 7-point sweep + R/B ratio 7-point monotone + candle <2222 K + 2222/2223 K continuity + D65 near-neutral + noon sun + extreme blue + max-temp + firelight-clamp), CascadedShadow (14: lambda-0 uniform + lambda-1 log + 0/1/2/3/4-cascade counts + clamp-to-max + endpoints exact for all n/lambda combos + build monotone + cascade boundary continuous + fit-matrix non-identity + up-fallback no-NaN), ClusterGrid (16: configure/reconfigure resize + directional fill + log mapping + overflow counted-not-silent + point XY-all-tiles + slice_z_range inverse round-trip + clamp-near/far/beyond + first/last z_range bounds + zero-range assign + clear no-reallocate + multi-assign accumulate + index boundary max-corner + overflow no-corrupt), Light struct (13: 112 B layout + ClusterCell layout + enum values GPU contract + flag distinct bits + default mask + directional normalize/color/lux/degenerate-zero-dir + point defaults + spot precompute/degenerate/inv-zero + rect_area orthonormal/bitangent exact + range 20). Zero TODO/FIXME/placeholder in all 5 headers. **SEALED (charter): `disk_area()` factory absent — `kDiskArea` enum + field layout in place, factory is a 10-line addition deferred to first consumer; ClusterGrid overlap with cd::lighting_clusters documented (not merged); no .cpp (header-only by design)** |
| cd::volumetric | 100 | charter-complete | 889 LOC/5 hdrs — charter scope = header-only CPU-reference + GLSL-string (VolumetricFog 445 inject/integrate/composite froxel; fog/Fog.hpp 128-slice Frostbite variant; clouds Schneider Perlin-Worley march; render/volumetric closed-form single-scatter; fx umbrella). **~101 tests** (+42 ADD-ONLY host regression locks this pass, all run everywhere no device): **VolumetricFog froxel** (cell_count degenerate-1x1x1/zero-depth/1e9-no-overflow; froxel_to_view centre-cell mirror-about-axis + aspect-scales-Y-only [tanY=tanX/aspect]; view_to_froxel zero-aspect-finite-guard + on-axis→centre-index; inject_cell HG-phase forward>backward + albedo per-channel-tint + finite-at-g→0.999-singularity + zero-dt→zero-RGB-keeps-σ_t; extinction/scattering clamp-net-negative; beer_lambert zero-distance-unity; FroxelGrid::at read/write round-trip vs index; integrate GLSL dt=(far-near)(s1²-s0²) numeric-equals-CPU-slice_thickness; composite GLSL sqrt/clamp/uIntegrated/imageLoad slice-mapping parity), **fog/Fog.hpp** (GridConfig 160x90x128 defaults + slice_to_view_z endpoints + view_z_to_slice clamp + slice round-trip; integrator stops-at-span-length undersized-dst + single-slice-spans-full-range; inject/integrate GLSL raw-scatter-dt-inside-loop parity tokens), **clouds** (Settings authored-layer defaults; height_fraction zero-at-boundaries + symmetric-about-midpoint + bounded[0,1]; remap clamp-both-directions + degenerate-zero-width-finite + inverted-output-range; density zero-outside-layer + coverage-threshold-boundary + linear-in-density_scale + monotone-rises-with-coverage + finite-across-noise-sweep; GLSL two-octave-blend/(1-coverage)/exp(-d·step)/trans<0.01 host-parity), **render/volumetric** (FogParams energy-conserving σ_s≤σ_t defaults; transmittance clamp-negative-product; in_scattering finite-at-zero-extinction-guard + albedo-tint; integrate_along zero-distance/zero-steps→zero + negative-σ_t-clamp + refines-toward-closed-form). No froxel/fog/cloud/single-scatter math or GLSL string changed — golden BYTE-IDENTICAL, ADD-ONLY. **SEALED (charter, ADR-grade): the RHI froxel inject→integrate→composite GPU dispatch is the documented out-of-charter seal. The three embedded compute kernels (kVolFogInjectCS/kVolFogIntegrateCS/kVolFogCompositeCS) ARE the byte-equivalent GPU contract the CPU reference mirrors (slice-thickness dt, premultiply-at-inject, scene·T+fog composite — all pinned by parity tests), but giving them a live .cpp DispatchPass (the cd::ddgi/restir_di pattern: image3D rgba16f alloc + sampler3D/UAV-barrier wiring + linear-depth bind + per-frame view-proj push + hello_engine HDR-composite seam) is a multi-week GPU-render subsystem that FEEDS THE RENDERED FRAME — activating it adds fog in-scatter that alters every shipped golden image, so it requires a dedicated GPU render-review (FLIP/SSIM golden refresh + perf budget), NOT a unit-test gate. Same seal class as cd::ddgi Sprint-7 / restir_di Sprint-7. The CPU froxel grid + integrator is the verified reference; the parity tests lock CPU↔GLSL so the GPU port stays byte-faithful when promoted. cloud ray-march + closed-form single-scatter are complete CPU contracts (no GPU seam needed). header-only by design (no .cpp)** |
| cd::atmosphere | 100 | production | ~470 LOC header; Hillaire CPU transmittance-LUT baker (40-step) + GLSL CS, + multi-scattering LUT (§5.3 Eq. 10 geometric series: 64 Fibonacci-sphere dirs x 20-step march, reads sealed transmittance LUT) + **now + sky-view LUT (§5.4: 30-step view-ray march, phased direct sun via `rayleigh_phase`/`henyey_greenstein` × sealed transmittance LUT + isotropic `Psi` from sealed multi-scatter LUT, opt-in/off-default-path)** + `sample_transmittance`/`sample_multiscatter` helpers + `kMultiScatterCS` + `kSkyViewCS`; 27 tests (+8 ADD-ONLY sky-view: shape / finite+non-neg / zero-scatter→0 negative / Rayleigh-blue-dominates-away-from-sun / brighter-toward-sun (phase isolation at fixed zenith row) / sealed-tables-read-only snapshot-compare / negative-altitude-clamps-finite / GLSL contract) over the prior +9 (transmittance + multi-scatter oracles). 3 of 4 LUTs baked + GLSL-mirrored + test-pinned. Transmittance + multi-scatter bakers BYTE-IDENTICAL (only ADD-ONLY funcs/tests; sky-view is opt-in, no renderer bakes it → default sky/golden unchanged). aerial-perspective LUT (4th, 3D froxel) + RHI dispatch SEALED promote-on-need (HDR-composite depth-consumer + golden-image harness); see `docs/ADR/ADR-20260621-atmosphere-lut-scope.md` |
| cd::denoise | 100 | production | 210 LOC header; edge-aware a-trous (Dammertz 2010 B3-spline 5x5) CPU + GLSL CS; 42 tests (21 pre-existing + 21 charter-complete ADD-ONLY: AtrousSettings defaults lock, all-three-stops combined weight, normal-stop full-suppression, normal-monotone 4-point, near-zero-sigma guards colour/depth/normal, three-channel rotational invariance, depth-stop sign symmetry, corner-pixel boundary-clamp, ping-pong even/odd iteration correctness, atrous_iteration step-size widens support, wsum boundary fallback, GLSL 9-token contract lock, OidnFilterKind enum-value pin, OIDN output-size all-kinds, OIDN non-uniform pass-through); a-trous math + GLSL BYTE-IDENTICAL (golden-safe, ADD-ONLY throughout). OIDN backend FORMALLY SEALED: API slot declared (OidnFilterKind enum + denoise_oidn()), body is compile-time identity stub; activating requires vcpkg oidn>=2.0 + CD_ENABLE_OIDN + OidnBackend.cpp TU + GPU golden fixture (non-deterministic across drivers, needs NVIDIA CI job). RHI dispatch SEALED out-of-charter: activating kAtrousCS requires framegraph slot + descriptor-set convention alignment + SVGF temporal accumulation pass (Schied 2017) — spatial-only would regress vs current TAA path; needs dedicated GPU render-review |
| cd::decal | 100 | production | 143 LOC header; world→decal-OBB projection + conservative AABB binning + GLSL helper; **37 tests** (6 original + 15 add-only-r1 + 15 add-only-r2 + 1 add-only-r3 [G6 abs-cull lock]); charter-complete: projection (OBB-axis dot-products, edge/centre/corner UV, atlas_uv_rect remap, rotated OBB, non-uniform extents, translated position, degenerate atlas, diagonal-outside, UV-not-written-when-outside, negative world coords, two-decal independence, 90°-roll axis swap, large-extents fractional UV); binning (touching-face inclusive, contained, single-axis-gap reject, 45°-widens-world-AABB, zero-size, translated, decal-inside-tile, single-corner-touch, negative-X-gap, negative-coord decal, asymmetric-extents-rotated, all-6-faces-touch, CONSERVATIVE FALSE-POSITIVE SEAL [B16: AABB-vs-world-AABB returns true for OBB-corner regions that project_world_to_decal rejects]); GLSL (struct layout + padding + function signature + UV bias + abs-cull on all 3 axes [G6]); struct invariants (BlendMode GPU-UBO stable values, default fields, atlas_uv_rect full-atlas default). No projection/binning math or GLSL change (golden BYTE-IDENTICAL). SEALED: (1) atlas/blend GBuffer pass + RHI dispatch — activates decal rendering, alters output, needs GPU render-review (multi-week); (2) full 15-axis OBB-SAT upgrade from conservative AABB-vs-world-AABB — tightens false-positives without correctness change, out-of-charter, locked by B16 (when promoted, B16 flips EXPECT_TRUE→EXPECT_FALSE making the change visible). |
| cd::virtual_textures | 100 | production | 139 LOC header; PageId/AtlasSlot/PageTable/feedback types + GLSL; **39 tests** (+33 ADD-ONLY host-math depth on the EXISTING types [run everywhere, no device]): PageId per-field equality + default-origin + max-field-distinctness (0xFFFF x/y + 0xFF mip as separate pages + all-max-equals-itself); PageIdHash disjoint-band layout [mip bits0-7 / y bits8-23 / x bits24-39] + max-fields->0xFFFFFFFFFF no-overlap + field-discrimination + band-boundary proof (y<<8 tops bit23 / x<<24 starts bit24 / AND==0) + max-y-cannot-alias-min-x (0xFFFF00 vs 0x1000000) + max-mip-stays-bits0-7 (no bleed into bit8) + hash-origin-is-zero; AtlasSlot default-valid==0 (pin actual struct vs ~0u comment) + allocated-valid==1-exactly + row-major n%w/n-div-w square + non-square + wrap-to-next-row + last-slot-coords (3x2 idx5->(2,1)) + lookup-returns-coords; FIFO evicts-oldest + reuses-victim-slot + no-evict-on-resident-realloc + 3-step ABCD chain (evicts A then B C+D survive) + last-allocated-survives-longest (1-slot atlas 5 allocs) + single-slot-1x1-atlas (2nd alloc evicts 1st slot-coords reused); distinct-mip independent-slots + max-mip-0xFF independent; resident-count-capped + fresh-table-empty + exact-capacity-fill-no-eviction (3x2=6 fill all 6 still resident per-page); lookup-unallocated-mip->null + lookup-adjacent-xy->null (negative); residents()-is-live-not-snapshot; large-atlas 16x16 (idx255->(15,15) + 257th FIFO oldest-evicted + newest-resident); GLSL structural contract (layout-set=0-binding=5 / struct-FeedbackReq / uvec4-pkg / full-function-sig / uint-slot=atomicAdd / F.reqs[slot].pkg) + GLSL-well-formed-string_view (non-null-data / size>0 / no-embedded-NUL). No PageId/hash/slot/feedback math or GLSL string changed (golden BYTE-IDENTICAL ADD-ONLY). **STREAMING RUNTIME FORMALLY SEALED**: VT streaming loop (async-IO page decode + RHI sparse-binding upload + per-frame GPU feedback readback + indirection-table GPU update) is explicitly out-of-charter. Activating requires: (a) cd::io + platform sparse-file I/O; (b) RHI sparse-binding (Vulkan VkSparseImageMemoryBind / D3D12 Tiled Resources Tier 2); (c) staging-buffer ring + CPU consumer thread; (d) GPU indirection-table update pass. None are stubs -- multi-week dedicated GPU render-review workstream (analogous to restir_di/ddgi dispatch-pass pattern). PageTable/FIFO/hash is the verified CPU reference future GPU systems mirror. SEALED until a dedicated VT-streaming epic is planned. |
| cd::velocity | 100 | production | 83 LOC header-only math primitive; motion-vector math (clip->uv delta) + velocity VS/FS GLSL; **33 tests/~78 asserts** (+12 ADD-ONLY host-math locks round 2 [run everywhere, no device]: large-w perspective compression (same-frame->zero), large-w ratio shift matches contract, motion-vector exceeds [0,1] — NOT clamped (TAA consumer must handle), negative clip-x with large-w correct sign, motion_pixels non-square anisotropic extent, motion_pixels always non-negative, static object w!=1 perspective->zero, VS Vulkan y-flip pin (gl_Position.y negated) + FS uses un-flipped varyings, VS push_constant layout pin (prev_vp_model/curr_vp_model/in_pos at location 0), FS out_motion at location 0, RHI-dispatch-sealed-out-of-charter recorded). No motion-vector math or GLSL change (golden BYTE-IDENTICAL — feeds TAA/motion-blur). Header-only by charter (INTERFACE CMake target, no .cpp). **RHI velocity-pass GPU dispatch FORMALLY SEALED**: activating the pass (pipeline create + render-pass bind + RG16F attachment write + per-mesh draw loop) alters the TAA/motion-blur consumed motion buffer; requires GPU render-review (FLIP/SSIM golden-image diff on post_composite output); jitter-removal (subtract TAA half-pixel offset before perspective divide) is a post_composite concern; mirrors seal pattern for cd::denoise / cd::decal / cd::virtual_textures / cd::light_shafts / cd::gpu_particles. Math + GLSL are the complete charter-deliverable; host helper is byte-identical to FS contract (pinned by GlslContractMirrorsHostMath + GlslVsYFlipContractPin + GlslVsPushConstantLayoutPin + GlslFsOutMotionAtLocation0) |
| cd::virtual_geometry | 100 | charter-complete | 1081 LOC/3 hdrs (ClusterDAG 474 BFS + GpuDispatcher 462); 66 tests (+47 ADD-ONLY host-logic locks on the EXISTING code, no production change, golden BYTE-IDENTICAL): **ClusterDAG/AABB/Cluster/Builder** (AABB inverted-sentinel default + per-axis expand + centre-midpoint + half_diagonal sqrt3/zero; Cluster valid() at/over 128-cap + root-sentinel defaults; ClusterDAG empty→0-levels/vacuous-acyclic + lod_levels=max+1-with-gaps + acyclic strict-parent both-direction violations; Builder fine→coarse node order + root parent_lod-sentinel/+inf reset + LOD0 self_error=0 vs coarser bbox-diagonal≥0 + finite inherited parent_error + child_count==0 documented + every-cluster-valid+acyclic + converges-to-single-root each-level-non-empty [NOTE per-level count NOT monotone: BFS re-cluster can transiently grow 6→8] + deep 4096-tri ≥3-LOD + single-triangle lone-leaf-root + deterministic-repeat-build; **BFS partition contract**: leaf cover is a SUBSET (drops degenerate zero-area pole-fan triangles, 241 of 256) no-dup/in-range; builder is CONNECTIVITY-preserving — a connected ≤128-tri fan stays in ONE leaf cluster, does NOT hard-split at the triangle cap; two vertex-disjoint triangles seed two leaf clusters; leaf-bbox encloses all member vertices); **GpuDispatcher host** (unconfigured-no-cull + reconfigure-clears-visible + LOD-frontier keep/drop-parent-fits/drop-self-too-large + distance-monotone near-keep/far-drop + threshold-sweep [0,N]-bounded + mesh_task_groups=0 under NullDevice no-mesh-shader + reset-when-nothing-visible + render-before-cull-parked + empty-DAG-noop + deep-built-DAG render==!empty + pack sentinel-safe/7-bit-mask-wrap/constexpr); **VirtualGeometry** (projected-error radius-linear/zero-radius/vp-height-linear/dist-clamp-finite; frontier self-too-large/root-+inf-always-passes-upper; pick empty/ascending-order/all-parent-acceptable-drops/cross-check-vs-is_lod_frontier). CHARTER = cluster-DAG construction + GPU cluster dispatch + screen-space-error LOD pick — all three SHIPPED, tested, golden BYTE-IDENTICAL. ⚠ LOD simplification is bbox-diagonal placeholder + midpoint-collapse skeleton — **real QEM (Garland–Heckbert 1997) FORMALLY SEALED as multi-week offline-geometry work (ADR-20260621-virtual-geometry-qem-seal): per-vertex quadrics + PQ edge-collapse + locked-boundary group simplification + per-cluster error bound; 3 in-code/in-test seal markers**; bbox-diagonal screen-space error is the CURRENT CONTRACT (CPU cull is the bit-for-bit reference the shipped kClusterCullCS mirrors). mesh-shader draw path stays no-op when backend lacks the extension (live Vulkan mesh-shader draw = separate GPU render-review, not part of the seal) |
| cd::ibl_gpu | 100 | charter-complete | 354 LOC INTERFACE header; real RHI upload helpers (cubemap/prefiltered/BRDF-LUT, float_to_half, barriers). **charter scope = the upload helpers**, REAL and fully pinned: **31 tests** (3 device-gated upload→readback + 28 host-side, run everywhere no device): float_to_half exact-reference-bit-patterns + sign-bit + overflow-clamp/underflow-flush + power-of-2 exponent ladder + smallest-normal(2^-14→0x0400)/largest-finite(65504→0x7BFF) saturation + **exponent-transition seams pinned BOTH sides** (2^-15 flush vs 2^-14 normal via std::nextafter, 2^16 saturate vs 2^15 clean-normal 0x7800) + **encoding monotone across the whole positive range** (integer ladder, no float-loop-induction) + **0x7BFF decodes to exactly 65504** vs independent reference + truncation-not-round-to-nearest + **full mantissa-truncation sweep** (low 13 bits dropped, half-ULP window→same pattern, crossing flips 0x3C00→0x3C01) + inf/NaN clamp-to-0x7BFF [not specials] + float-subnormal flush-to-signed-zero + round-trip stable vs INDEPENDENT reference decoder + sign-bit-independent-of-magnitude; descriptor/staging/barrier/copy-region/view sizing: cubemap 6-face RGBA16F byte math + prefiltered cumulative mip byte-offsets [3072/3840/4032] strictly-increasing + BRDF-LUT 2-channel-half bytes + prefilter mip-count cap at kMaxSpecularMips + cube barrier range {0,1,0,6} + prefiltered barrier range mip_count tracks product + **prefiltered copy-region count = 6 per mip (24 for 4-mip, last targets mip3/face5/1px)** + BRDF single-full-extent region + default-output-POD invalid-handles/mip_count-1 + single-mip prefiltered roughness-0 mirror + empty cube/prefiltered/BRDF zero-mip guard). All new tests pin ACTUAL float_to_half + uploader byte arithmetic vs IEEE-754 binary16; no header/API change (behavior-preserving). GPU upload→readback round-trip (BRDF-LUT copy_image_to_buffer→download byte-compare + cubemap/prefiltered mip-count) stays Vulkan-device-gated (GTEST_SKIP without ICD). **SEALED (charter, ADR-grade): the live hello_engine IBL-composite integration — wiring these uploaded cube/LUT views into the HDR composite's split-sum specular + irradiance sampling — is a downstream RENDERER consumer, out-of-charter for the upload library. Activating it adds indirect IBL to the rendered frame (alters every shipped golden), so it is multi-week renderer-graph wiring + a dedicated GPU golden-image review, NOT a TODO of the upload path. The upload helpers ARE the contract and are byte-verified end-to-end on a live Vulkan device; same seal class as restir_di Sprint-7 / ddgi composite seam.** |
| cd::gpu_particles | 100 | charter-complete | 103 LOC header; CPU advance + compact_alive + one simulate GLSL CS; **20 tests/~60 asserts** (semi-implicit Euler order, per-axis integration, dt scaling, lifetime decrement, alpha-fade clamp + max_life=0 floor, zero-dt/empty/mixed-live edges; compaction stability/idempotence/single-element/all-live; GLSL host-parity contract pins; default-particle state); GPU emit kernel + host-side indirect-draw buffer ownership SEALED (multi-week GPU-driven-particles RHI consumer integration, not a TODO) |
| cd::light_shafts | 100 | charter-complete | 108 LOC header; sun-screen-projection + Mitchell radial-blur GLSL; **50 tests** (+28 ADD-ONLY comprehensive cover [run everywhere, no device]: **Settings default-value locks** (samples=64/decay=0.96/density=0.95/weight=0.5/exposure=0.15 exact); **sun_screen_pos deep cover** (non-unit direction magnitude-invariant, fz-just-above-zero valid, fz-just-below-zero sentinel boundary, left/right symmetry equidistant from 0.5, up/down symmetry equidistant from 0.5, aspect=0.5 doubles horizontal offset, tilted-camera roll-90 exact uv); **radial_blur_accum deep cover** (zero-samples no-fetch returns-zero, one-sample exact, start==sun all-fetches-at-sun-uv, weight=0 yields-zero, exposure=0 yields-zero, decay=0 only-first-step-contributes, decay=1 uniform-il N*field exact, off-screen start_uv passed-through un-clamped, decay-product closed-form cross-check); **GLSL contract locks** (kRadialBlurCS: pc.samples/pc.weight/pc.density/pc.sun_uv/pc.size names, local_size_x=8/local_size_y=8, imageStore(dst, push_constant qualifier; kInlineConeShaftGlsl: sun_intensity/strength params, vec3/sun_color; epipolar-seal test: neither kernel contains epipolar substring)). No math or GLSL change (golden BYTE-IDENTICAL). **FORMALLY SEALED -- ADR-grade rationale for analytic epipolar deferral (Engelhardt & Dachsbacher 2010 / Kim & Marsalek 2014):** requires epipolar-line generation per light in screen space + per-pixel depth-aware attenuation integral along epipolar lines + GPU scatter/gather with epipolar-line interpolation + depth-discontinuity handling -- a separate multi-week algorithm, NOT a TODO of the Mitchell radial-blur path. Radial-blur (O(N) screen-space post-process) covers the shipped visible look. Promote-on-need trigger: sunset-grade finely-detailed shafts through complex occluders -- add epipolar as SECOND selectable path alongside (not replacing) radial blur. ADR-20260616-band6-render-misc-scope.md §3 codifies this. RHI dispatch SEALED (activating alters rendered output, needs GPU render-review) |
| cd::nrc | 100 | cpu-mlp-v1 complete + GPU backends sealed | INTERFACE header; single-hidden-layer CPU MLP (Muller NRC): forward `W_in·x+b_in→ReLU→W_out·h+b_out` + half-squared-error SGD backward + **`train_batch` mini-batch SGD** (mean per-sample gradient vs frozen weights — the GPU fully-fused backward shape) + `query_into` (byte-identical scratch-reusing zero-heap inference) + `encode_input` (Muller §3.2 frequency/positional encoding, 5-D raw → 32-wide feature). **27 tests** (was 16): forward EXACTLY vs independent reference (asymmetric input + zero-input bias-through-ReLU + ReLU clamp proven exercised + 5 widths 1..128), kOutputDim=3/kInputDim=32 contract, batch purity, large-magnitude finite; TRAINING strict — ≥50% L1 drop + converge <0.05, single-sample overfit→~0 loss (1e-3), monotone descent e0>e100>e200, zero-input fits via bias only, higher-LR-converges-faster, single big step toward target, zero-error step exact no-op; **ADDITIVE** — query_into byte-identical + reused-scratch correctness, encode_input determinism/finite/zero-pad/lowest-octave-sin-cos/large-bank-clamp/feeds-network, **train_batch single-element==train_step (to float tol)** + empty no-op + mean-error ≥50% descent + determinism. MLP forward/backward math VERIFIED vs Muller model. **CUDA-NN/OneAPI/SPIR-V GPU backends SEALED** — documented stubs only (no code; real 16-wide fully-fused tensor-core MLP + Adam + GPU freq-encoding + per-frame online training co-scheduled with the path tracer is multi-month + needs a CUDA/SPIR-V toolchain + no consumer; focused Iglberger seal **ADR-20260621-nrc-gpu-backend-seal**, original ADR-20260616 §4) with a promote-on-need gate. CPU MLP is the verified reference oracle for a promoted backend; charter-complete CPU, GPU radiance cache deliberately deferred |
| cd::restir_gi | 100 | charter-complete (reservoir-math done+tested; GI-trace SEALED per ADR-20260621) | 401 LOC header; real WRS reservoir update/combine/clamp/weight/temporal_blend CPU + matching GLSL helper + 3 embedded CS (sample/temporal/spatial). 5 happy-path tests + **44 ADD-ONLY host-math tests** [run everywhere, no device] — full parity with + BEYOND cd::restir_di's test_restir_di_reservoir_math.cpp depth, pinning ACTUAL behaviour: final_weight closed-form + M==0/pdf<=0 guards + inverse-pdf scaling + **M==1 smallest-nonzero**; update negative-weight short-circuit + strict swap-ratio threshold + incoming-radiance carry + **the GI-specific inline `weight_sum>0` guard (running-negative-sum keeps blocking then recovers once strictly positive — distinct from DI update's early `<=0` return)** + **multi-stream WRS determinism** (replay bit-identical, pure fn of weight/rand pairs); combine empty-donor no-op + additive-M growth + zero-p_hat M-only fold + **the w = p_hat·weight_sum/pdf M-cancellation identity** (verified independent of donor M) + visibility re-test flip (+ **whole-Sample point/normal/incoming carry, only `valid` forced 0**) + non-positive-pdf zero-weight + **positive-weight grow-but-keep when rand≥ratio**; clamp_history strictly-greater guard (no-op at/under cap) + **fires-at-cap+1 proportional scaling** preserving final_weight; temporal_blend alpha endpoints + tie-at-0.5→current + **0.5+ε→previous boundary** + midpoint lerp + **non-half lerp + uint M truncation toward zero** + current-age carry; invalidate full reset; **Sample/Reservoir member-init defaults (normal {0,0,1}, valid 1, empty Reservoir)**; GLSL helper/3-kernel presence + binding locks. Reservoir math + GLSL BYTE-IDENTICAL (golden-safe). Math VERIFIED vs Ouyang 2021 / Bitterli 2020 RIS. **CHARTER-COMPLETE per the honest-100% rule: reservoir-math-v1 IMPLEMENTED + COMPREHENSIVELY host-tested; the GI sample CS DEFERRED-TRACE (synthesised bounce + constant cached radiance, NOT a real ray query) is FORMALLY SEALED with a focused Iglberger-format ADR + precise promote-on-need gate — docs/ADR/ADR-20260621-restir-gi-deferred-trace-seal.md (same seal CLASS as restir_di Sprint-7). A real GI gather needs G-buffer + TLAS ray-query + a second-bounce radiance cache wired by a render-graph consumer (the cd::restir_di::DispatchPass pattern), multi-week RHI-dispatch greenfield with no current indirect-lighting consumer. Three DEFERRED-TRACE seal markers + the focused-ADR cross-reference locked by tests so the honest-scope banner cannot be silently dropped.** |

**Group rollup: ~61%.** Two tiers: a STRONG RHI-wired GPU set (restir_di/ddgi/post/lighting_clusters/cluster) and a MID header-only "CPU-reference + GLSL-string" set whose on-screen behaviour depends on the consumer (hello_engine) compiling the embedded GLSL — so library-level % overstates end-to-end readiness. WEAK: nrc/light_shafts carry explicit placeholders for their core work (virtual_geometry raised to 100/charter-complete: cluster-DAG construction + GPU cluster dispatch + screen-space-error LOD pick all shipped + host-tested + golden byte-identical; only the QEM mesh-simplification step is FORMALLY SEALED as out-of-charter offline-geometry work per ADR-20260621-virtual-geometry-qem-seal; restir_gi raised to 100/charter-complete: reservoir-math-v1 implemented + COMPREHENSIVELY host-tested (44 ADD-ONLY, full parity with + beyond restir_di's reservoir-math depth), only the GI sample-CS trace stays a DEFERRED-TRACE FORMALLY SEALED with a focused promote-on-need ADR per ADR-20260621-restir-gi-deferred-trace-seal; ibl_gpu raised to 100/charter-complete: the charter = RHI upload helpers (cubemap/prefiltered/BRDF-LUT staging + float_to_half + barriers/copy-regions/view descriptors), all host-pinned by 31 tests (full IEEE-754 binary16 + sizing) with the GPU upload→readback round-trip device-gated; the live hello_engine IBL-composite integration is a downstream renderer consumer FORMALLY SEALED out-of-charter (alters every shipped golden, needs multi-week render-graph wiring + golden review); gpu_particles raised to 100/charter-complete: CPU advance+compact_alive+kSimulateCS comprehensively host-tested (54 tests); emit/indirect SEALED ADR-20260616-band6 §2; volumetric raised to 100/charter-complete: froxel-fog inject/integrate/composite + Schneider cloud march + closed-form single-scatter all comprehensively host-tested (~101 tests, +42 ADD-ONLY this pass) + CPU↔GLSL byte-parity locked, only the live RHI froxel inject→integrate→composite DispatchPass FORMALLY SEALED out-of-charter — it feeds the rendered frame, needs a dedicated GPU render-review). **Systemic caveat: froxel light-cluster binning is duplicated 3× (cluster ↔ lighting_clusters ↔ light::ClusterGrid).**

---

## 6. engine/world

| Lib | % | Tier | Basis |
|---|---|---|---|
| cd::ecs | 100 | production | ~2.1k LOC hdr-inline (sparse-set World + Scheduler 407 + ArchetypeWorld 543 side-layer + SystemGraph); 179 .cpp; 101 gtests/6 files; dual storage, no stubs. Smallest-pool `each<>` driver REALISED (was dead MVP code) + snapshot-safe mid-walk mutation; edge/negative/stress depth: generation-recycle invariants, tombstone reuse, large-id sparse growth, non-trivial-dtor lifetime nets through swap-pop + cross-archetype migration, scheduler acyclicity invariant, query-cache structural invalidation; README de-lied (no phantom deferred-lifecycle/commit API) |
| cd::scene | 100 | production | ~2.1k LOC/21 hdrs (Scene graph + Serializer 314 + BinarySerializer 259 + Frustum/SpatialHash/Light/Trigger); 137 gtests (+51 edge: JSON/binary serializer corrupt/truncated/forward-compat branches, frustum exact-plane boundary, spatial-hash dense/negative/boundary cells, trigger re-entry, deep-hierarchy transform compose, detach-mid-tree, LOD/Layer/NameRegistry/Polyline degenerate); no transform/cull/query change (golden-safe) |
| cd::anim | 100 | production | ~1.7k LOC hdr-inline (DualQuat/Kavan, StateMachine, BlendTree2, PoseBlend, Skeleton, GpuSkinning); 100 gtests (+39 ADD-ONLY edge in new test_anim_edge.cpp: DualQuat identity/zero-weight/renorm, StateMachine guard/self-loop/dup-reject, BlendTree2 clamp/NLERP-antipodal, PoseBlend count-mismatch/clamp, Skeleton empty/bind-pose/hierarchy, GpuSkinning palette correctness); no pose/skinning math touched (golden-safe) |
| cd::anim_ik | 100 | production | 492 LOC CCD solver + joint limits (Sprint-2); 294 hdr; 19 gtests; real convergence; edge/negative/boundary: already-at-target(0 iters), zero-length bone, diagonal single-joint, max-iter cap, exact-min-clamp, exact-max-clamp, all-zero-bones NaN-free, 3-joint already-at-target; FABRIK sealed as Sprint-3 (out-of-charter) |
| cd::gameplay_input_binding | 100 | production | 326 LOC ActionMap (axis/button binding, threshold heuristics, dead-zone, clamp, just_pressed/just_released); 264 hdr; 33 gtests; is_action_just_pressed + is_action_just_released + set_dead_zone(per-action) + axis clamp to [-1,1]; full edge/negative/boundary coverage |
| cd::gameplay_time | 100 | production | 82 LOC Time.cpp (game clock/fixed-step/scale); 154 hdr; fixed-step accumulator (sub-step/catch-up/spiral-clamp) + scale-0/neg + pause edge tests; 10+ gtests |
| cd::input | 100 | production | 818 LOC/9 hdrs (Axis/Hold/DoubleClick/KeyChord/Gamepad/MouseDrag) + thin 38-LOC pump; 86 gtests; boundary/edge/negative/simultaneous sealed (phase1121) |
| cd::net | 100 | production | ~2.4k LOC hdr-inline (ReliableChannel 390/Retransmit 566/QosDispatcher/SnapshotReconciler/Delta/Prediction/RLE) + real UdpConnection 254 (winsock); 147 gtests (+58 ADD-ONLY in new test_net_edge.cpp: ack-window lost-ack+>64-shift, RTO backoff, SACK-cap, snapshot gap-stops-replay, prediction correct-missing, RLE worst-case+truncated, seq u8/u32 wraparound, window exact-boundary, QoS byte-cost+token-burst-cap, delta out-of-range, throttle neg-dt, snapshot out-of-order); no wire-format change |
| cd::physics | 100 | production | ~900 LOC/14 collision primitives (Aabb/Obb/Capsule/Sphere/Ray/Triangle/Sweep/Inertia + IPhysicsWorld); 194 .cpp; OBB/OBB 15-axis SAT added (was documented-deferred); 101 gtests (+46 depth pass) — full edge/negative coverage per primitive (just-touching/concentric/degenerate/parallel/grazing/behind/TOI t0..t1/analytic inertia) |
| cd::net_lobby | 100 | production | 464 src (Lobby 168+destroy_room+host-migration / Packet 132 / SocketTransport 133); 507 hdr; 40 gtests (28 Lobby + 7 Packet + 5 SocketTransport); RoomState.host_player_id + host-migration + destroy_room sealed; full edge/negative coverage (join/leave/full/duplicate/passcode/host-migrate/empty-room/destroy/game-started-gate/active-rooms/multi-room-isolation/monotonic-ids/solo-start/ready-toggle) |
| cd::world_container | 100 | production | header-only 946 LOC/7 (LevelStreamer 188 residency + ProjectIo 363 JSON + Level/Layer/Project/World); 51 gtests (+32 gap-closure: World name, Layer color_tag/draw_order/postfx/persistent, Level find_missing/const/scene_path/remove-OOB/active-OOB, Project empty-access/OOB/const/settings-defaults, ProjectIo malformed-JSON/version-0/version-as-string/missing-name/non-object-level/missing-scene_path/missing-layers/wrong-type-settings/bad-bounds-size/negative-active_layer/OOB-active_layer/empty-round-trip, LayerMember overlong-truncate/empty-world/clear-unassigned/rename-no-match, LevelStreamer empty-project/same-level-reload/tracked-count-multi-switch/pre-activate-state); full edge/negative/boundary coverage sealed |
| cd::audio | 100 | production | ~1.6k src/6 backends (WASAPI 543/CoreAudio 302/ALSA 275/FileSink/Null/Native); 145 gtests (+59: voice state-machine/loop-wrap/gain/pitch, mixer accumulation, FileSink amplitude/sum/saturate/clamp, PanLaw/PitchShift/Limiter/Compressor). 3 REAL BUGS FIXED: Voice loop-wrap lost fractional overshoot (timing jitter on looping clips), FileSink set_volume + set_master_volume unclamped (>1.0 amplitude); platform-gated, Win-verified |
| cd::audio_spatial | 100 | production | 705 src (HrtfMixer 437 + AudioSpatial 268); 50 gtests (+27 gap-closure: 3 attenuation curves with math verification, all-model boundary d==inner/d==outer, same-position NaN guard, behind/above listener panning, Doppler stationary/zero-velocity/formula/clamp/both-moving, gain 0/2 scaling, unknown-id compute/update/remove negative, duplicate-add overwrite, rotated-listener front, exponential edge cases, multi-model coexist); AttenuationModel enum (kLinear/kInverseSquare/kExponential) + rolloff_factor field added; behaviour-preserving (kLinear default) |
| cd::net_matchmaker | 100 | production | ~530 .cpp (SkillScorer + FillStrategy + MatchmakingQueue); ~480 hdr; 35 gtests (+24 gap-closure: MatchTicket queue enqueue/cancel/expire/status, duplicate-guard, exact-fill seal, skill-window widening logical-clock, priority ordering, FIFO fairness, region-gate in queue, kStratified strategy, antipodal haversine, zero-weights, history-symmetry, join-closed/leave-absent/multi-lobby negatives); TicketStatus + MatchResult + MatchmakingQueue added; behaviour-preserving for all existing API |
| cd::physics_soft_body | 100 | production | ~600 .cpp PBD/Verlet + Sprint-2 self-collision (spatial-hash + impulse) + Sprint-2.5 Provot bending constraints + half-space ground-plane collision (Coulomb friction); ~330 hdr; 29 gtests (+19 depth: analytic Verlet free-fall sum, neg-dt no-op, damping-removes-energy, gravity-sign, empty/zero-G guards, over-stretch/compress/zero-stiffness/zero-rest-length constraint, bending fold-resist + empty-baseline-identity + invalid-index, ground stop/fall-through/zero-normal/friction/pinned-obstacle, self-collision single-particle/zero-radius); shared project_distance() PBD helper; opt-in bending/ground keep Sprint-1/2 byte-identical; GPU compute + FEM sealed (documented, out of mass-spring charter) |
| cd::sample_framework | 100 | production | 118 src (App lifecycle 105 + run 22); 176 hdr; 44 gtests (5 original M2A + 39 gap-closure: lifecycle ordering/double-init/dtor-safety/shutdown-during-boot, FrameContext monotonicity/dt/total_time accumulation, frames_pumped()/has_run() accessors, AppConfig defaults, run-template multi-arg forwarding, request_shutdown idempotency/ordering edge cases); double-init guard + frames_pumped() + has_run() added to src+hdr; full edge/negative/boundary coverage sealed |
| cd::net_session_replay | 100 | production | 230 .cpp SRPK record/replay binary format; 149 hdr; ~25 gtests (+record→replay round-trip bit-identical, magic/version mismatch, truncation per-field, empty/single/large session, replay-past-end, corrupted payload, reset). SRPK wire format unchanged |
| cd::physics_jolt | 100 | production | 1164 .cpp real Jolt 5.x backend (CD_PHYSICS_JOLT_REAL via FetchContent) + Euler stub fallback; 414 hdr PIMPL; 29 gtests (14 + 15 depth: non-positive dynamic mass clamped-to-unit, monotonic-handle no-alias, transform+velocity round-trip, gravity + zero/negative-dt no-op, static/kinematic ignore forces, free-fall damped-band velocity, null/stale-handle safety, collider isolation, joint prune, 32-body independence, rebuild residue-free, foreign-world rejection). **2 REAL BUGS FIXED (exposed by the new tests): (1) create_body next_idx_ started at 0 → the first body's BodyHandle packed to value 0 = cd::core::Handle null-sentinel → is_valid()==false despite the body being usable; now starts at 1. (2) step() passed dt straight to JPH::PhysicsSystem::Update, which asserts/UB on dt≤0 → added a non-positive-dt no-op guard.** real-Jolt vehicle-constraint validity SEALED behind the vendor flag |
| cd::audio_dsp_fx | 100 | production | header-only DSP (311: biquad filters + Schroeder/Jot FDN reverb) + anchor .cpp; ~25 gtests (+filter freq-response DC/Nyquist/cutoff + stability, FDN decay/RT60 + no-blowup, impulse energy, reset-clears-state, denormal/div-zero guards). SIMD path SEALED (scalar is the correctness contract) |
| cd::physics_vehicle | 100 | production | 638 src bicycle model (longitudinal) + dynamic linear-tyre single-track LATERAL model (understeer-gradient yaw correction, grip-clamped lateral accel) + Jolt WheeledVehicle delegation; ~30 gtests (+throttle/brake/drag, gear/rpm, left/right yaw symmetry, steer→yaw monotonic at low speed, understeer non-monotonic yaw-vs-speed, grip saturation). Full Pacejka + Jolt-vendor path SEALED |
| cd::particle_system | 100 | production | 297 LOC SoA CPU sim (semi-implicit Euler + exponential drag + max_particles cap); 48 gtests (+Euler pos math, dt scaling, lifetime boundary, SoA coherence multi-swap-pop, simultaneous spawn+death, burst, fractional accumulator, velocity spread bounds, flat size/color, degenerate spread, empty-system snapshot, particle_count==snapshot-written, erase-only-particle self-swap, double-remove idempotent, zero-rate emitter, negative life clamped to epsilon, monotonic EmitterId, orphan particles age-out, multi-tick accumulation, t-clamp near death, drag exponential math, zero-drag backward-compat, negative-drag clamp, max_particles cap enforced, cap=0 unlimited, cap refills after death, two-emitter independent caps, rapid add/remove cycle, drag+lifetime independence, drag monotonic velocity decay). **2 CPU features added: drag (v *= exp(-drag*dt) per tick, exact exponential, stable any dt) + max_particles cap (per-emitter hard limit, 0=unlimited, refills on death)**. GPU dispatch + force fields SEALED (multi-week, documented) |

**Group rollup: ~83%.** Mature and uniform — ~700 gtests cluster-wide, all libs ship real impl. Caveats are all documented: particle_system CPU-only by scope (GPU=V3); physics_jolt now pins its DEFAULT Euler-stub fallback at 100 (deterministic, full edge/negative coverage) with real-Jolt-only assertions SEALED behind CD_PHYSICS_JOLT_REAL; physics_vehicle still reaches the real Jolt backend only with the vendored build flag; audio_dsp_fx defers SIMD/FDN tuning. The remaining stub-text hits are confined to those documented vendor-gated fallback paths, not silent gaps.

---

## 7. engine/ui

| Lib | % | Tier | Basis |
|---|---|---|---|
| cd::ui_layout | 100 | production | Flex + Constraint; incremental 3-phase solver (compute_main/compute_cross/position_children real impl); kAbsolute inset placement; margin main+cross; kDuplicate detection; 51 gtests (23→51, +28 edge/negative/error-path); gap_cross sealed (multi-line only, no-wrap scope) |
| cd::ui_animation | 100 | production | 213 src + 232 hdr; full Penner easing (quad/cubic/elastic/back/bounce) + Tweener + Timeline, cited; 29 gtests (14→29: +all-curves endpoint sweep, +back bound, +elastic overshoot, +bounce unit-interval, +InOutQuad/Cubic symmetry, +OOB clamp, +negative-dt, +tick-after-done, +default-constructed, +empty-timeline, +invalid-channel, +negative-start-time, +seek-via-reset, +overlapping-channels); std::numbers::pi_v + emplace_back hygiene |
| cd::ui_input | 100 | production | 559 src + 450 hdr; HitTester (z-order) + FocusManager (tab + modal stack) + GestureRecognizer FSM (press/drag/long/double/pinch/swipe); 42 gtests (21→42: +5 HitTester z-order/nested/boundary/neg-coord, +8 FocusManager blur-modal/pop-empty/clear/removed/invalid-id/no-subchain/preserve-valid/double-push-pop, +8 GestureRecognizer drag-boundary/long-exact/double-exact/pinch-in/swipe-left/swipe-down/slow-no-swipe/reset-mid) |
| cd::ui_theme | 100 | production | 175 src + 293 hdr; Material 3 tokens, dark/light/high-contrast + brand override + WCAG on-color + spacing/motion/elevation; 48 gtests (22→48, +26 gap-close: WCAG boundary 3:1/4.5:1/7:1/21:1 + Rec.709 channels + extremes; pick_on_color above/below/exact 0.179; brand partial-override + double-override; theme_from_name dark/light/hc/unknown/empty; spacing+motion+elevation monotonic all 3 themes; token unit-range completeness; distinct primaries; HC on_primary dark; lerp no-NaN + non-color from-to; name-constant round-trip) |
| cd::ui_renderer | 100 | production | 140 src + 220 hdr; CPU batcher, quad/textured/glyph emit, u16 indices, scissor stack, adjacent-merge; 20 gtests; kMaxVertices=65532 guard (silent drop + at_vertex_limit() query, sealed+tested); no auto-split (explicit rationale in header) |
| cd::ui_renderer_rhi | 100 | production | 712 src + 240 hdr; ring vb/ib over cd::rhi + Route A (caller pipeline) + Route B glslang inline solid-quad + material UI-variant; 28 gtests (12→28, +16 gap-close: ZeroMaxIndices + both-zero capacity, full-cap create, vertex/index upload byte-exact sizing, exact-cap boundary + one-quad-over reject, empty-batcher zero-state, no-clip+explicit scissor record, default-constructed inert-safe (upload/set_*/record/destroy guards), upload-after-destroy, set_* no-op on plain-create, Route-A inert-variant kInvalidArgument reject, move-construct + move-assign ownership, re-upload overwrite; +1 GPU-gated create-time atlas binding writes SDF descriptor + idempotent re-bind); 15 host-side cases run on NullDevice (no GPU gate), GPU draws stay behind the Vulkan device gate. GLYPH ATLAS host path IMPLEMENTED + tested (create-time `info.atlas_view`/`atlas_sampler` write + `set_sdf_atlas` rebind); real-pixel glyph rendering belongs to hello_ui on a presenting swapchain (not in any golden scene) |
| cd::ui_widgets | 100 | production | 6797 LOC, 8 widgets (ColorPicker 786/CurveEditor 819/DockSpace 939/Table/TreeView/PopoutDock) + NativeWindowAdapter (208, Win32-only); 141 gtests/9 files (+39 gap-close: HSV hue-wrap/clamp/greyscale-preserve/RGB-primary-round-trip/ColorF-byte-round-trip/on_change-release-only/hex-lowercase; CurveEditor set_keyframe-move+OOB/kFree-no-overwrite/remove-curve-clamp/view-range-guard/name-color/tick-drag; DockSpace find_panel_owner/has_panel/ratio-clamp/drop-kNone/restore-truncated/set_active_tab-clamp; Table clear_selection/CellRange-contains/set_columns-evict/header-no-op/set_row_count-clamp/column-layout; TreeView set_nodes-resets-sel/clear_selection/root_count-multi/expand-leaf-no-rows/click-below-clears/nested-expand; PopoutDock multi-panel-order/reattach-one-of-many/drag-size-unchanged; NativeWindowAdapter stub-sizes-zero/multi-open-close); X11/Cocoa cross-platform gap SEALED (Win32-only per Sprint-2/cd::platform gating; stub path covers non-Win32 CI) |
| cd::ui_font | 100 | production | 1528 LOC; stb_truetype always-on raster + skyline pack + MSDF + optional FreeType/HarfBuzz/msdfgen behind gates; 28 gtests (15→28, +13 default-path depth: GlyphSlotPixelsNonZeroForRenderedCodepoint, GlyphMetricsBearingAndAdvancePresent, AtlasUvInRangeAndOrdered, SpaceGlyphHasZeroBitmapButPositiveAdvance, MissingGlyphReturnsNullopt, SkylinePackedGlyphsDoNotOverlap, AtlasFullRejectWhenMaxDimTooSmall, KerningOnLoadedFontIsFiniteNocrash, MsdfModeOnStbBackendDefaultBuild, SelectAtlasModeAfterRasterizeIsNoop, LoadTtfConvenienceAliasEquivalent, IdentityShapeAdvanceSumMatchesPerCodepointAdvances, RasterizeRangeIdempotentForOverlappingRange); complex HarfBuzz/FreeType shaping SEALED with rationale (multi-week vendor prerequisite; identity+stb raster is the correct default-build contract) |
| cd::ui_a11y | 100 | production | 153 src + 251 hdr; A11yTree register/meta/tab-nav + constexpr WCAG contrast + focus rect; ~25 gtests (+register/unregister/find/meta, tab-nav fwd/back/wrap/skip-disabled, focus rect, WCAG 3:1/4.5:1/7:1 AA/AAA boundaries, nested/remove-focused/contrast-extremes). OS AT bridge (screen-reader/AT-SPI/UIA) SEALED — separate multi-week OS-integration effort |
| cd::ui (umbrella) | 100 | production | Widget.cpp 52 + 393 hdr (Panel/Label/Button) + 9 header widgets; 91 tests (57→91, +34 gap-close: +disabled widget hit-test skip/draw-still-emits/dispatch-skip+enabled accessor; +z-order 3-sibling; +empty-tree hit+draw; +id round-trip; +Button empty-label rect-only+label accessor; +Label color accessor+draw+set_text; +Panel background draw color; +Tooltip reset+delay accessor+negative-delay clamp; +ProgressBar total accessor+set_total clamps done; +Spinner default-angle+speed_rps accessor; +TabBar empty active=nullptr+clear+tabs()+active_index; +ContextMenu clear_items+items span; +ToastAnim 8 phase tests (age0/steady/fade/expired/fromLeft/fromTop/fromBottom/overload)+2 stack-offset; +Anchor center-odd-rounding+stretch-nonzero-origin; GLYPH LAYOUT SEALED (DrawKind::kText carries raw string; glyph metrics+atlas UV belong to cd::ui_font)); Widget gained enabled_/set_enabled(bool) — disabled skips hit_test+dispatch but still draws |
| cd::editor (shell + editor_ui + 27 panels) | 70 | partial | ~15.8k src / ~10k test / 515 cases (497→515: +18 gap-close in test_editor.cpp — EditHistory: undo-empty/redo-empty/byte-budget-eviction/null-push/empty-label/redo-label; SelectionSet: sorted-order/remove-absent/remove-last-primary/re-add; CommandPalette: OOB-invoke/null-action/word-boundary-rank/empty-palette; HierarchyView: multi-root-depth0/collapse/clear/depth3); shell wires ecs::World+scene+input+widget tree; 27 panels bind real engine systems (IkChainEditor 756, Cutscene 555) BUT most render simplified DrawBatcher visuals — only Inspector uses ImGui; ImGui render surfaces SEALED (headless-untestable) |
| cd::ui_renderer_webgpu | 57 | partial | 306 src + 184 hdr; default CD_UI_WEBGPU_HAVE_DAWN=0 path is pure no-op (counts verts only); Dawn path allocates ring vb/ib + issues DrawIndexed per command but WGSL pipeline/shader is SEALED (multi-week, real Dawn CI runner required); 25 gtests covering stub-path contract: create/lifecycle/upload/record/destroy + zero-limit reject + vertex/index overflow + exact-capacity boundary + empty-frame zeroing + successive re-snapshot + scissor multi-command + record-before-upload + move-construct + stub-handle nullity + glyph emit counts + textured_quad counts + mixed-variant command splitting + upload-after-destroy safety + move-assign + same-key merge + different-slot split + batcher vertex-limit reflection + SubmitterCreateInfo defaults + record-after-destroy no-op + scissor stack depth tracking + rejected-upload preserves prior state; Dawn/WGSL GPU pipeline SEALED with detailed rationale (SetPipeline/SetBindGroup deferred to hello_ui_webgpu Phase 5.5). |

**Group rollup: ~72%.** Clean architecture (renderer-agnostic widget tree → batcher → RHI/WebGPU). Strong feature libs (layout/input/animation/theme). The honest gap is feature-completeness/visual-fidelity, not unimplemented skeletons: the editor is the biggest single area and most LOC-overstated (real bindings + exhaustive tests, but placeholder visuals + no real in-panel text); webgpu backend is a no-op stub with thoroughly tested host-side accounting (25 gtests, SEALED Dawn/WGSL pipeline); default font build has no shaping.

---

## Overall Engine Completion

| Group | Libs | Rollup % |
|---|---|---|
| engine/foundation | 20 | ~79% |
| engine/asset + runtime + tooling | 15 | ~64% |
| engine/game | 20 | ~86% |
| engine/render — CORE | 15 | ~84% |
| engine/render — FEATURES | 19 | ~61% |
| engine/world | 21 | ~81% |
| engine/ui | 12 | ~72% |
| **ENGINE TOTAL** | **122** | **~76%** |

**Weighting method: lib-count-weighted mean of all 122 per-lib %s (equal weight per library).** This is the honest baseline. Note two distortions if you re-weight: (a) **LOC-weighting would push the number UP** — the single biggest lib, cd::rhi (24.5k LOC, now 100%), plus the large game/world/render-core production libs dominate code volume, while most of the FEATURES laggards are tiny header-only files; a LOC-weighted estimate lands closer to ~80-82%. (b) **End-to-end "shippable feature" weighting would push it DOWN** — many FEATURES-tier libs are header-only algorithm+GLSL-string with no standalone GPU runtime, so their library-% overstates on-screen readiness until the consumer wires them. The ~76% lib-count figure is the most defensible single number for planning.

---

## Strongest / Weakest / Highest-Leverage

**Strongest (real, deep, well-tested):**
- **cd::rhi — 100%.** Three genuinely-deep backends (Vulkan/D3D12/Metal), 75+46-virtual interface, 340 tests with a cross-backend pixel-parity + readback-parity capstone. The crown jewel. (⚠ stale header banners lie about completeness — fix them so the next reader isn't misled.)
- **engine/game (~86%) and engine/world (~81%)** as whole clusters — uniformly production, every lib has substantive .cpp + documented formats + citations + a dedicated gtest binary.
- Standout individual libs: cd::concurrency, cd::math, cd::ecs, cd::scene, cd::material, cd::gluon, cd::restir_di, cd::post.

**Weakest (explicit placeholders for core work):**
- (cd::nrc raised 40→70: CPU MLP forward + SGD backward math now deeply host-tested — 16 tests pin exact forward-vs-reference agreement, ReLU clamp, determinism, single-sample overfit→~0 loss, learning-rate effect, zero-error no-op — and the production CUDA-NN/OneAPI/SPIR-V backends are explicitly SEALED documented stubs, not a partial GPU radiance cache.) (cd::restir_gi raised 38→70: reservoir math now deeply host-tested; the GI *trace* stays a documented DEFERRED-TRACE seal — ray-query gather is a multi-week RHI-dispatch subsystem.)
- **The three asset streamers (texture/scene/audio, ~45%)** — real queue+threading, but decode is an explicit Sprint-2 placeholder (fake 1×1 handles / synthetic IDs).
- **cd::ui_renderer_webgpu (57%)** — default path is pure no-op with thoroughly tested host-side accounting (25 gtests); Dawn/WGSL GPU pipeline SEALED (multi-week, deferred to hello_ui_webgpu Phase 5.5).
- **cd::ibl_gpu (70%, 21 tests).** (cd::gpu_particles raised 45→70: CPU advance + compact_alive now have a 20-test pin; GPU emit + indirect-draw wiring SEALED. cd::light_shafts raised 42→70: sun-projection + radial-blur-v1 now have a 16-test host-math pin; analytic epipolar Kim&Marsalek SEALED out-of-charter. cd::virtual_geometry raised 52→70: ClusterDAG build/AABB/Cluster + GpuDispatcher cull/render/sizing + projected-error/frontier/pick now have a +47-test host-logic pin; real QEM mesh simplification SEALED as multi-week offline-geometry work, bbox-diagonal screen-space error is the current contract.)

**Highest-leverage for the new plan:**
1. **Asset streamer decode backends** (3 libs × ~45% → production) — they already have the hard part (orchestration) done; wiring real WAV/glTF/cdtex decode unlocks actual streaming and lifts the whole asset cluster.
2. **De-duplicate froxel clustering** (cd::cluster ↔ cd::lighting_clusters ↔ cd::light::ClusterGrid) — the same binning exists 3×; consolidating removes a standing maintenance hazard and clarifies ownership.
3. **The editor (68%, ~15.8k LOC)** — biggest single surface; the bindings and tests exist, so investment in real text/glyph rendering + ImGui-izing panels converts a lot of latent work into visible DCC capability.
4. **FEATURES GPU-wiring tier** (denoise/atmosphere/velocity/decal/virtual_textures, 55-72%) — these are CPU-reference + GLSL-string libs; giving them real .cpp RHI dispatch (the cd::ddgi/restir_di pattern) closes the "library-% vs end-to-end" gap that most inflates the current baseline.
5. **Refresh cd::rhi header banners** — near-zero effort, prevents the next planner from re-baselining off "boot-only/stubbed" comments that are now false.