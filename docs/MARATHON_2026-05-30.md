# Marathon 2026-05-30 — UI library family + Sponza fix-up

Session date: 2026-05-30
Branch: `dev`
Status: shipped phases 425-464; 122/122 tests PASS; build clean on
`ninja-debug`.

## Per-phase count

40 substantive phases shipped this session (phase numbers as recorded
in commit messages; some phases reused numbers from earlier audit
windows because the audit was retroactive):

```
phase425-vis2(prim.frag)             gate ambient floor on any_light
phase426-vis3(prim.frag)             reduce shadow bias slope (Sponza)
phase427-vis4(hello_engine)          restore binding=4 after Sponza loop
phase428-vis5(hello_engine)          fix mesh_for / blas_for_kind
phase429-arch(adr)                   UI widget lib + gameplay lib ADRs
phase430-flatten1(ui)                engine/ui/ui -> engine/ui top lib
phase431-flatten2(render)            engine/render/render -> engine/render
phase432-naming-sweep(refactor)      final naming audit + findings
phase433-vis6(sample+shader)         NaN clamp + SSBO bounds guard
phase434-vis7(sample)                per-Sponza sandstone SSBO tint
phase435-vis8(sample)                disable editor floor on Sponza
phase436-veg(material+sample)        vegetation alpha-test + double-side
phase437-black(shader+sample)        comprehensive black-fragment fixes
phase438-sync(sample)                regenerate PrimShader inline
phase439-gltf(asset+test)            SceneLoader + LoadedScene + 6 gtests
phase440-ingest(render+test)         SceneIngest + TextureUpload + 4 gtests
phase441-cov(test+doc)               SceneIngest tests 4->9 + README
phase442-ui-layout(lib+test+doc)     cd::ui::layout Flexbox + 14 tests
phase443-ui-font(lib+test+doc)       cd::ui_font stb_truetype + 5 tests
phase444-ui-renderer(lib+test+doc)   cd::ui_renderer DrawBatcher + 10
phase445-ui-int(test)                cd::ui Phase 1 integration smoke
phase446-vis9(sample+shader)         tint.w==4.0 glTF prim sentinel
phase447-ssr(shader+composite)       SSR per-prim gate via surface_flag
phase448-arch(asset+sample+shader)   data-driven per-prim PBR (glTF mat)
phase449-bloom(post)                 threshold 1.0->3.0 + intensity tune
phase450-exp(sample)                 exposure 3.0->1.0 + saturation
phase451-rt+csm(shader+sample)       bias scale-aware for Sponza 0.01
phase452-gltf(asset+sample)          normal + MR + emissive + occlusion
phase453-ui-rhi(lib+test+doc)        cd::ui_renderer_rhi Submitter + 6
phase454-exposure(lib+test+doc)      cd::post_exposure kernel + 12 tests
phase455-sponza-fix(shader+sample)   is_floor gate + Sponza light pos
phase456-perprim-normal-mr(sample)   per-prim normal + MR descriptor
phase457-ui-input(lib+test+doc)      cd::ui_input HitTester + Focus
phase458-ui-animation(lib+test+doc)  cd::ui_animation easings + Tweener
phase459-gameplay-time(lib+test+doc) cd::gameplay::time TimeKeeper
phase460-ui-widgets(lib+test+doc)    Button/TextInput/Slider/Toggle/...
phase461-exposure-gpu(post+test)     GPU compute reduction skeleton
phase462-gameplay-input-binding      cd::gameplay::input_binding ActionMap
phase463-hello-ui-sample(samples)    hello_ui ties cd::ui Phase 1+2 libs
phase464-marathon-summary(docs)      THIS DOCUMENT
```

Plus one mid-session ADR commit (`docs: ADR-20260530-auto-exposure-pipeline`)
that documents the GPU companion to phase454.

## Tests start / end

* Start of session (commit `bc7d686` baseline): test count was lower; the
  cd::ui_layout / ui_font / ui_renderer / ui_renderer_rhi / ui_input /
  ui_animation / ui_widgets / gameplay::time / gameplay::input_binding
  / post::exposure suites all landed in this session.
* End of session: **122 / 122 PASS** under `ctest --preset ninja-debug
  --output-on-failure`.

Test deltas attributable to this session (counted by the phase commit
messages):

* `cd_test_flex`                +14
* `cd_test_font`                +5
* `cd_test_batcher`             +10
* `cd_test_submitter`           +6
* `cd_test_ui_input`            tests added by phase457
* `cd_test_animation`           tests added by phase458
* `cd_test_ui_widgets`          tests added by phase460
* `cd_test_gameplay_time`       tests added by phase459
* `cd_test_gameplay_input_binding` tests added by phase462
* `cd_test_post_exposure`       +12 (phase454) + GPU skeleton coverage (phase461)
* `cd_test_ui_phase1_integration` integration smoke (phase445)
* `cd_test_scene_loader`        +6 (phase439)
* `cd_test_scene_ingest`        +4 then 5 more (phase440 / phase441)

## Libraries added

Eight new public cd::* libraries shipped this session:

1. `cd::ui_layout`       — Flexbox solver (Yoga semantics; row/col/justify/align/grow/shrink/gap/padding/margin)
2. `cd::ui_font`         — stb_truetype glyph atlas + skyline bin-pack
3. `cd::ui_renderer`     — CPU DrawBatcher (Vertex/Index/DrawCommand triple + scissor stack)
4. `cd::ui_renderer_rhi` — Submitter (ring vb/ib + descriptor set + RHI bridge)
5. `cd::ui_input`        — HitTester + FocusManager (tab/shift-tab + modal capture)
6. `cd::ui_animation`    — easings + Tweener + Timeline
7. `cd::ui_widgets`      — Button / TextInput / Slider / Toggle / Checkbox / Dropdown / Modal
8. `cd::gameplay::time`  — TimeKeeper (fixed-step + scaled time)

Plus extensions to existing libs:

* `cd::gameplay::input_binding` — ActionMap (phase462; whether this counts as new is taste-dependent)
* `cd::post::exposure`          — CPU auto-exposure kernel (phase454) + GPU compute skeleton (phase461)
* `cd::asset_gltf`              — SceneLoader / LoadedScene with normal/MR/emissive/occlusion (phase439, 452)
* `cd::render::scene_ingest`    — SceneIngest + TextureUpload (phase440)

CMake reports `Libraries declared: 110` end-of-session (was lower at
start; exact delta tracked by phase commit log).

## Architectural deliverables

* **ADR-20260530-ui-widget-library** committed phase429. Specifies the
  full UI tier: Phase 1.0 layout (Yoga-shape Flex), 1.1 font (stb -> FT
  + HarfBuzz in Phase 4), 1.2 renderer (CPU batcher), 1.2b renderer_rhi,
  Phase 2.1 input + 2.2 widget catalog, Phase 3 animation. Eight of the
  promised libraries shipped this session.
* **ADR-20260530-auto-exposure-pipeline** committed mid-session as the
  GPU companion to phase454; phase461 implemented the compute reduction
  skeleton.
* **Naming sweep (phase432)** — final audit of cd_<lib> target /
  cd::<lib> alias / cd/<lib>/ include / CD_<LIB>_API export macro
  consistency; ten-plus inconsistencies documented + retired.
* **engine/ui/ui -> engine/ui flatten (phase430)** and engine/render/render
  -> engine/render flatten (phase431). Trims one directory level off
  every consumer include path.
* **Sponza rendering visual quality** — phases 425, 426, 433, 434, 435,
  436, 437, 438, 446, 447, 448, 449, 450, 451, 452, 455, 456 collectively
  walked from "scene loads but black fragments + wrong shadows" to a
  data-driven per-prim PBR pipeline with proper bias scaling and
  vegetation alpha-test.
* **glTF -> scene pipeline (phase439, 440, 441, 452)** — `cd::asset_gltf::
  SceneLoader` produces a `LoadedScene` POD with mesh / material /
  texture indices; `cd::render::scene_ingest::SceneIngest` uploads the
  textures + builds the renderable mesh. Both fully unit-tested.
* **hello_ui sample (phase463)** — first end-to-end tie-together of
  every Phase-1/Phase-2 cd::ui library, gated by Vulkan in CMake and
  with a NullDevice fallback so CI can smoke-test without a GPU.

## Known limitations + queued items

### hello_ui

* Live Vulkan validation (validation-layer-clean fragment output)
  not exercised this session — needs a host with a Vulkan ICD and
  the GPU-validation harness. The sample compiles + boots clean on
  NullDevice; the Submitter::record path is touched on each frame
  but the actual GPU draws are no-ops under NullDevice.
* Glyph rendering not yet wired in the sample — `cd::ui::font::Font`
  is loaded and the atlas is rasterized, but the sample only emits
  background panels for the leaf rects. Wiring `Font::glyph_uv` to
  `DrawBatcher::glyph` per-codepoint is straightforward and queued
  for the hello_ui v2 pass.
* No swapchain integration in the sample — the Vulkan branch
  creates a command buffer but does not present. Adding
  `cd::render::Renderer::begin_frame / end_frame` (same shape as
  hello_imgui) lifts this to a fully drawing demo.

### UI tier

* Phase 3 (animation) shipped easings + Tweener + Timeline (phase458)
  but no widgets currently consume them. Tween hooks queued for
  Phase 2.2-followup.
* Phase 4 (FreeType + HarfBuzz, MSDF, variable fonts, BiDi shaping)
  not started.
* `cd::ui_input` ships HitTester + FocusManager but no gesture
  recognizers (double-click / drag / long-press) — Phase 2.2+.
* `cd::ui_widgets::Modal` shipped but the dropdown / modal stack
  integration with the FocusManager is documented-but-not-tested at
  the integration tier.

### Auto-exposure

* `cd::post::exposure` GPU compute skeleton (phase461) is API only;
  the actual reduction pipeline is not wired into the composite
  framegraph. CPU path (phase454) is fully tested + integrated.

### Sponza

* Per-prim PBR is data-driven from the glTF material catalog (phase448)
  but the Sponza ground-truth is reference-only — no golden-image
  diff CI gate yet.
* Vegetation alpha-test (phase436) is enabled; alpha-to-coverage
  variant for AA is queued.

### Tests

* No new tests for the hello_ui sample itself (it is a sample, not a
  test). Its compile-cleanliness IS the CI gate.

### Architecture

* Phase 463 marks the first time every Phase-1 + Phase-2 UI library
  is exercised from one binary. Phase 5 (constraint solver / grid /
  RTL / WebGPU backend etc.) remains future work.

## Build + test attestation

```
cmake --build --preset ninja-debug          # ninja: no work to do.
ctest --preset ninja-debug --output-on-failure
  ...
  100% tests passed, 0 tests failed out of 122
  Total Test time (real) =  23.50 sec
```

End-of-session HEAD: `phase464-marathon-summary` (this commit).
