// cd::foundation_utils umbrella — compile-only smoke that proves the
// aggregator header picks up every member lib's main TU.
#include <cd/foundation_utils/foundation_utils.hpp>

int main()
{
    return 0;  // success — TU compiled.
}
